# Workspace Agent sidebar review fixture

Isolated workspace root for the registered `workspace-agent-sidebar` behavior spec.
The review server enables the persisted scripted two-Agent harness; the generic
UI-review runner owns temporary workspace and session directories.
