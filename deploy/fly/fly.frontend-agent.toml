app = "boring-ui-frontend-agent"
primary_region = "cdg"

[build]
  dockerfile = "../shared/Dockerfile.ts-backend"

[env]
  APP_ENV = "production"
  NODE_ENV = "production"
  AGENTS_MODE = "frontend"
  AUTH_DEV_AUTO_LOGIN = "false"
  AUTH_DEV_LOGIN_ENABLED = "false"
  CONTROL_PLANE_PROVIDER = "neon"
  CONTROL_PLANE_APP_ID = "boring-ui"
  AUTH_SESSION_SECURE_COOKIE = "true"
  BUI_APP_TOML = "/app/boring.app.toml"
  BUI_AGENTS_MODE = "frontend"
  DEPLOY_MODE = "core"
  WORKSPACE_BACKEND = "bwrap"
  AGENT_RUNTIME = "pi"
  AGENT_PLACEMENT = "browser"

[http_service]
  internal_port = 8000
  force_https = true
  auto_start_machines = true
  auto_stop_machines = "off"
  min_machines_running = 1
  processes = ["app"]

  [http_service.concurrency]
    type = "requests"
    soft_limit = 200
    hard_limit = 250

  [[http_service.checks]]
    grace_period = "20s"
    interval = "15s"
    method = "GET"
    timeout = "5s"
    path = "/health"

[vm]
  cpu_kind = "shared"
  cpus = 1
  memory = "512mb"
