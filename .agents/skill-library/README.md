# Skill Library

Inactive and third-party skill sources retained for reference. Pi does not
auto-discover this directory.

- Active project skills live in `../skills/`.
- Pinned provider methods used by active skills live in `../skill-references/`.
- Copy or adapt a library skill into `../skills/<name>/` only when it is ready to
  become an intentional project workflow.
