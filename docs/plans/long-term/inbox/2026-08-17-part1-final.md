Part 1 — Business Use Cases and Required Platform Capabilities

Status: Consolidated business and capability thesis
Purpose: Define the jobs Boring may be hired to do, the capabilities those jobs require, the trust and ownership boundaries that must remain coherent, and the option space that the future architecture must preserve.
Out of scope for this section: implementation sequencing, MVP versus post-MVP decisions, repository migration, package layout, database schema, concrete interfaces, service decomposition, and final naming of architectural objects.
0. Executive summary

Boring has one foundational product family:

    A purpose-built, sovereign, agent-native application in which humans, Agents, automations, and authorized external clients can perform durable domain work through the same governed operations.

That product family can be hired for three overlapping jobs:

    Operate: help a person or organization perform domain work through normal software, with an Agent underneath.

    Distribute: turn an expert’s method, Agent, knowledge, tools, and application experience into repeatable software for other private contexts.

    Improve: compare candidates and methods against evidence, real outcomes, and an incumbent, then promote or reject improved revisions safely.

These are not three platforms and must not become three parallel object models.

They are one application family moved along independent dimensions:

Distribution
private application
→ reusable package
→ installed expert product
→ subscription / external consumption

Adaptivity
human-only
→ Agent-assisted
→ personalized
→ evidence-aware
→ outcome-driven
→ controlled recursive improvement

A creator-published SaaS application sits far along both dimensions. An industrial optimizer sits far along the adaptivity dimension. A CRM with an ambient Agent may remain close to the beginning. A personal expert Agent may use a small chat-heavy experience while still relying on the same ownership, Work, operation, evidence, and package foundations.

The strongest shared business requirements are:

    the visible product feels like real domain software rather than an Agent console;

    the application remains usable when the Agent or model is unavailable;

    a bounded Agent job may begin in a Page, first-party chat, WhatsApp, email, Slack/Teams, voice, MCP, or an API, while the durable Work remains independent of the channel;

    meaningful human actions and Agent actions use the same governed domain behavior;

    Work, outputs, costs, approvals, effects, deliveries, and outcomes remain attributable and recoverable;

    outside content is treated as untrusted data, never as authority;

    calculation-heavy products use deterministic, versioned domain kernels rather than invented model arithmetic;

    private customer evidence does not silently become publisher knowledge or platform training data;

    changes are proposed as immutable revisions, compared with an incumbent, and activated only through authorized promotion;

    publishing, multi-Agent work, collaboration, multi-application composition, and cloud placement remain possible without dictating today’s user experience;

    usage and cost facts are captured consistently, while pricing, payer, billing unit, invoicing, and publisher settlement remain configurable by vertical and commercial offer;

    Boring’s own orchestrator-and-worker code factory uses the same Work, review, inbox, evaluation, and promotion paths as customer products;

    an independent developer can scaffold, test, and deploy an Agent or agent-native application through a stable framework and hosted deployment path such as npx boring create and npx boring deploy.

Part 2 will determine the smallest set of objects and relations capable of satisfying this business space. Part 1 deliberately does not turn every capability into an object.
1. Purpose, boundaries, and anti-goals
1.1 Why this section exists

Boring may eventually appear as:

    a creator studio;

    a CRM or mail product;

    a macro research terminal;

    an investment workbench;

    an industrial formulation system;

    a clinic operations product;

    an expert Agent sold to an audience;

    a channel-first Agent job service reached through chat, WhatsApp, email, Slack/Teams, voice, MCP, or API;

    a headless domain service used by another Agent;

    a developer-built Agent or application created locally and hosted on Boring;

    a collaborative multi-Agent environment;

    an orchestrator-and-worker code factory with review gates and a human inbox;

    or a sovereign European Agent cloud.

Those examples overlap heavily. They should be used to pressure-test the platform, not to mint one architecture per example.

This section answers:

    What jobs can customers hire Boring to perform?

    What can the users, publishers, subscribers, operators, and Agents do?

    What capabilities recur across those jobs?

    Which ownership and trust boundaries must be explicit?

    Which business dimensions may evolve independently?

    Which capabilities are structurally expensive to retrofit?

    What business possibilities must Part 2 preserve without prematurely building them?

1.2 What this section may constrain

This section may define:

    jobs users hire the product to do;

    business-level vocabulary;

    user-visible capabilities;

    ownership and trust boundaries;

    capability invariants;

    business success and failure conditions;

    preservation requirements that later architecture must not collapse.

It may not:

    turn every capability into an architectural object;

    prescribe a microservice or package layout;

    require a marketplace, one fixed billing model, a billing engine, multi-app operating system, or generalized optimizer;

    assume chat, Dockview, a workbench, or route-first SaaS is the universal shell;

    assume that every Source is a filesystem;

    assume that every application needs BSL or an explicit optimization ontology;

    choose the first commercial vertical;

    decide implementation order.

1.3 Anti-goals

The capability space is wide, but Boring is not intended to become everything.

Boring is not primarily:

    a consumer chatbot platform with plugins;

    a horizontal no-code application builder competing on breadth;

    a Zapier-like workflow automation product;

    a BI or semantic-layer company;

    a foundation-model provider;

    an ungoverned Agent framework;

    a public Agent marketplace or one mandatory per-token pricing model as its foundational product;

    an operating system for arbitrary untrusted third-party applications;

    a system whose moat depends on silently training on customer data;

    a universal event-sourced world model;

    or an optimization ontology imposed on mail, documents, CRM, and ordinary operational software.

Boring is a way to build and operate agent-native applications whose:

domain behavior
data
authority
Work
outputs
history
costs
outcomes
and improvement process

remain controlled by the application owner.

A non-goal may be revisited only through an explicit product decision, not by accidental accumulation.
1.4 Capability preservation classes

Business importance and architectural retrofit cost are different dimensions.

Part 1 uses three preservation classes. They describe how Part 2 must preserve the option, not when the capability must be implemented.
STRUCTURAL

The capability depends on an identity, boundary, or historical link that is expensive or unsafe to retrofit after customers have data.

Typical examples:

    tenant or Instance scoping;

    Actor identity on every consequential event;

    immutable revision identity;

    the join between execution, Artifact, cost, decision, and outcome;

    package lineage;

    untrusted-content provenance;

    operation effect classification;

    deployment-independent identity.

Part 2 must represent the necessary identity or relation, even if the user-facing feature is absent.
EXTENSIBLE

The capability can be introduced through a new driver, View type, trigger, evaluator, adapter, or service without migrating the core historical identity model.

Typical examples:

    a new CRM connector;

    a new chart renderer;

    a new notification channel;

    a new model provider;

    a new evaluation method;

    a public marketplace UI.

Part 2 must expose a clean extension point, but this capability alone should not justify a new root object.
PRODUCT

The capability is primarily a product or presentation choice and imposes little or no universal architectural constraint.

Typical examples:

    whether the Agent appears in a drawer or full page;

    whether the product uses tabs;

    mobile-native versus responsive web;

    exact pricing model;

    visual brand.

The preservation classes are not a roadmap and must not be interpreted as “build all STRUCTURAL capabilities first.”
2. Core business thesis and vocabulary
2.1 What an agent-native application is

An agent-native application:

    feels like familiar purpose-built software to a human;

    remains a complete usable product when its Agent is disabled, unavailable, or over budget;

    lets an authorized Agent perform the same meaningful domain operations as the human;

    admits work from humans, Agents, automations, services, messaging channels, developer clients, and external machine consumers;

    connects private and public data without surrendering credentials or ownership;

    preserves durable Work independently of a browser, model, process, or sandbox;

    produces citable and reviewable Artifacts rather than only transient model text;

    records approvals, edits, effects, failures, costs, and available outcomes;

    can become adaptive without requiring a new platform;

    changes improvable behavior through immutable revisions and controlled promotion.

The visible product may be:

Creator Studio
Pipeline Recovery
Mail
Macro Research
Investment Research
Formulation Lab
Clinic Operations
Document Intelligence
Expert Agent
Agent Job Service
Developer-Hosted Agent App
Headless Domain Service

The common capability is not a shell.

The common capability is:

    Actors use governed domain behavior over owned data to perform durable Work, produce Artifacts, allocate human attention, and learn from evidence.

2.2 Human-native outside, Agent-native inside

The human experiences domain nouns and normal application affordances:

Accounts
Campaigns
Forecasts
Formulations
Patients
Documents

The Agent experiences:

an authorized current-context projection
a discoverable domain contract
typed Operations
approved Sources
deterministic calculation capabilities
structured Artifact outputs

The Agent does not need to see the same pixels as the human.

Human/Agent parity means parity of:

    command;

    validation;

    authorization;

    effects;

    concurrency rules;

    idempotency;

    audit;

    outcome linkage.

It does not mean:

    every gesture is a tool;

    every Agent-only exploration becomes a button;

    the full operation catalog is injected into every prompt;

    the Agent controls the application by simulating clicks;

    the Agent is automatically authorized merely because an operation exists.

2.3 Level 0 — complete software without the Agent

Every serious domain application should have a human-only degraded mode.

At Level 0:

    routed Pages and deterministic Views load without a model call;

    users can inspect records and prior Artifacts;

    queued approvals can still be viewed and resolved where safe;

    deterministic domain calculations can still run;

    data can be exported;

    no new Agent work begins if the provider or budget is unavailable.

The Agent adds leverage. It must not be the single point of availability for the application.
2.4 Work as the durable unit of customer value

Chat messages are not the commercial unit.

The meaningful unit is Work:

ACME renewal
Eurozone inflation scenario
New supplier formulation
Q4 creator campaign
Portfolio concentration review

Work can have:

    a goal or request;

    context and linked Sources;

    human and Agent participants;

    one or more executions;

    Artifacts;

    approvals and questions;

    external effects;

    costs;

    delayed outcomes;

    a final status.

Work is the natural unit for:

    resumption;

    sharing;

    attention;

    provenance;

    cost attribution;

    ROI;

    evaluation;

    support and debugging.

A bounded Agent job is one commercial and interaction form of Work. It may define:

    a brief or requested result;

    input and Source references;

    constraints and exclusions;

    expected deliverables and formats;

    acceptance criteria;

    budget, deadline, or service level;

    allowed external effects;

    questions and approvals that may interrupt execution;

    delivery and notification preferences.

The platform must keep three moments distinct:

Produced
= the Agent generated an output

Delivered
= the customer received a valid durable deliverable under the agreed contract

Successful
= the deliverable later achieved the intended real-world result

A completed model turn is not necessarily completed Work. A delivered job requires a terminal Work status and one or more durable Artifacts satisfying the agreed output contract.

Part 2 may represent Work and conversational Thread as one object or as related objects, but must preserve the business concept independently of a model-native session or external channel conversation.
2.5 Business-level vocabulary

These definitions constrain meaning, not implementation. Part 2 may merge or split them only with an explicit reason.
Term	Business meaning
Actor	A human, Agent, automation, service, or authorized foreign client that may initiate or perform Work. A model process is not itself the durable Actor.
Agent	A named, versioned Actor whose behavior can operate through domain Operations under bounded authority.
Authority	What an Actor may do now, to which data or records, for which purpose, under which approvals and limits.
Operation	A typed domain command, query, calculation, or effect with shared behavior for human, Agent, automation, and API invocation.
Source	A governed connection to data or a service: files, database, mail, CRM, public data, SaaS, object store, or remote capability.
Domain Contract	The application’s discoverable nouns, verbs, identifiers, schemas, and effect semantics. It is broader and more universal than an analytical semantic model.
Semantic Model	A versioned analytical description of entities, measures, dimensions, relationships, units, and lineage over one or more Sources.
Work	A named, durable, resumable unit of customer activity containing context, participants, executions, Artifacts, and attention needs. A bounded job is one form of Work.
Job Contract	The business agreement for bounded Work: inputs, constraints, expected deliverables, acceptance criteria, budget/deadline, effects, and delivery preferences. It may be represented as Work metadata rather than a root object.
Channel	A first-party or external intake, steering, notification, or delivery surface such as app chat, WhatsApp, email, Slack/Teams, voice, MCP, or API. A channel conversation is not the Work identity.
Delivery	A terminal handoff of a valid Artifact or structured result to an authorized consumer. Delivery is distinct from production and later Outcome.
Thread	A conversational or event-oriented history associated with Work. Part 2 decides whether Thread and Work are one object.
Execution	One admitted attempt to perform Work, with a durable identity linking inputs, Actor, Agent revision, authority, costs, effects, and outputs.
Artifact	A durable, citable, versioned output with provenance: report, draft, dataset, candidate, forecast, formulation, View definition, or revision.
Attention Item	A human-facing request linked to Work: approval, question, review, exception, completion, escalation, or outcome confirmation.
Evidence	A signal linked to Work, Artifact, and revision. Includes preference signals, operational facts, and real-world outcomes.
Outcome	An observed result in the world, distinct from a human liking or approving an output. It may be delayed, noisy, contested, partial, or never observed.
Evaluator	A versioned procedure or human protocol used to compare an Artifact or revision against declared criteria. Its fixtures and held-out evidence are not readable or modifiable by the Actors it judges.
Revision	An immutable proposed replacement for an incumbent Agent, Experience, semantic definition, evaluator, workflow, package, or domain candidate.
Experience	The versioned human-facing definition of navigation, Pages, Views, actions, branding, and Agent presence.
View	A presentation of data, Work, and actions; declarative by default, trusted code by exception.
Page	A stable routed destination composed of one or more Views.
Package	A versioned distributable bundle of methodology, Agents, Experience, knowledge, semantic definitions, requirements, and optional trusted code.
Instance	An isolated running installation containing owner-controlled data, Work, credentials, local settings, and overlays.
Projection	A purpose-scoped representation of authorized data or capability for a user, Agent, process, or external consumer. A mount is one possible file projection.
Automation	A governed Actor or rule that admits Work from a schedule, event, Source change, or webhook.
Usage Fact	A measured cost or consumption event attributed to Work, Execution, Artifact, Actor, Package, Instance, or consumer. It is not itself a price, invoice, entitlement, or payout.
2.6 Three parties and four data categories

Distribution creates at least three parties:

    Instance owner: the customer whose private context, Work, and outcomes are being processed.

    Publisher: the creator, expert, company, or Boring team that authors the Package and default methodology.

    Platform operator: the operator of identity, hosting, policy, metering, and package infrastructure; this may be Boring, a dedicated operator, or the customer.

Some products also depend on third-party-licensed providers such as market data, public datasets, or SaaS APIs whose terms constrain use and redistribution.

The platform must keep four categories conceptually distinct even when infrastructure is shared:
Category	Meaning
Source data	Customer-connected or public/provider data. Owner-controlled, revocable, and governed by its own terms.
Knowledge	Publisher-packaged methodology, examples, fixtures, and instructions. Versioned with the Package; not a live copy of customer data.
Memory / preference	Scoped personalization for a user or Instance. It is not automatically evidence and never widens scope silently.
Evidence	Permissioned preference or outcome signals linked to Work and revisions. It is not retrieved as publisher knowledge unless explicitly approved for that purpose.

A subscriber fact copied into publisher knowledge is a privacy incident, not “learning.”
2.7 Explicit trust-boundary transfers

Nothing should cross an Instance, publisher/subscriber, or platform boundary merely because two systems share a database.

Cross-boundary movement should take an explicit business form:

    Projection: attenuated, purpose-scoped input, often read-only;

    Artifact: immutable output with provenance and citations;

    Evidence export: explicit, permissioned, preferably aggregated or fixture-shaped;

    Package: versioned methodology and Experience without live customer data;

    Entitlement: what an Instance may run, not what private data it contains.

If a cross-boundary flow cannot be explained through an explicit transfer form, the boundary is not trustworthy.
3. One foundational family, three business jobs

There is one foundational family: the agent-native application.

The following are three jobs the same platform can be hired to do. They remain separate in this business document because their buyers, scarce resources, interfaces, evidence, and commercial models differ.

They must not become three parallel architectures.
Job	Scarce resource	What must be true
Operate	Time, throughput, consistency	The product feels like normal software; humans and Agents share domain behavior.
Distribute	Expert time and audience reach	Publisher methodology is versioned; subscriber context is private and isolated.
Improve	Decision quality versus reality	Candidates, incumbents, evidence, delayed outcomes, and controlled promotion can be compared.
3.1 Job A — Operate domain work
Business statement

    I want to build purpose-built software for an individual or organization, where people perform normal domain work through a familiar interface and an authorized Agent can perform the same meaningful work underneath.

Representative products

    creator studio;

    SME pipeline tool;

    CRM;

    mail client;

    macro research terminal;

    investment workbench;

    document intelligence product;

    clinic operations application;

    industrial management interface;

    planning and scheduling product;

    analytics application.

What the user can do

The user can:

    navigate through domain-specific Pages and Views;

    create, inspect, update, and organize domain state;

    work with records, files, messages, documents, tasks, dashboards, and calculations;

    ask the Agent to act on current semantic context;

    let the Agent perform authorized domain Operations;

    preview or approve consequential work;

    receive background results through an attention surface;

    resume named Work on another device or after a process restart;

    inspect what happened, under which authority, with which sources and costs;

    optionally connect the Work to later outcomes;

    optionally compare improved Agent or Experience revisions.

What it should feel like

Purpose-built navigation
+
normal Pages and Views
+
records, documents, dashboards, and actions
+
ambient Agent and attention plane

The Agent may be hidden, ambient, available in a drawer, or visible on a dedicated page. The app remains visually and operationally primary.
Business value

    saved professional time;

    increased throughput;

    fewer missed tasks and exceptions;

    higher-quality analysis or decisions;

    safer and more consistent execution;

    access to specialized capability;

    better business or operational outcomes.

Commercial forms

    recurring SaaS;

    per-seat or per-Instance subscription;

    usage-based pricing;

    private deployment;

    design-partner service;

    enterprise contract;

    selected outcome- or value-linked models.

3.2 Job B — Distribute expert capability
Business statement

    I want to turn an expert’s method, Agent, knowledge, tools, and optional application Experience into repeatable software that other people can use with their own private context.

Representative products

    a creator’s content strategy Agent;

    a macro analyst’s research Agent;

    a consultant’s SME diagnostic Agent;

    a property expert’s analysis Agent;

    an investment expert’s company-research Agent;

    a personal CFO Agent;

    a domain expert’s technical review product;

    a full creator-published SaaS application.

What the publisher can do

The publisher can potentially:

    encode methodology as Agent definitions, knowledge, examples, deterministic tools, and workflows;

    derive the methodology from their own Work, edits, approvals, rejections, and successful Artifacts instead of writing everything upfront;

    provide a chat-heavy, minimal, or full SaaS Experience;

    declare required Sources and Operations;

    provide evaluators and example fixtures;

    publish immutable versions and change logs;

    communicate which capabilities and estimated costs the Package requests;

    improve the product from evidence that is explicitly permitted at publisher scope;

    distribute through an existing audience.

What the subscriber can do

The subscriber can:

    use the expert Agent or application;

    connect their own private Sources and credentials;

    retain private Work, memory, Artifacts, and outcomes;

    approve consequential Operations;

    apply local preferences and Experience overlays;

    choose whether to upgrade;

    export their data and Work;

    later expose selected capabilities to their team or another authorized client.

Product shapes

chat-first expert Agent
small purpose-built Agent application
full branded SaaS
embedded sidecar
headless expert service

Ownership promise

The publisher owns methodology and Package lineage.

The subscriber owns private context and local outcomes.

The platform owns infrastructure facts and only the telemetry permitted by policy.

Third-party data keeps its original license obligations.
Business value

The publisher gains leverage, recurring revenue, and productized expertise.

The subscriber gains persistent context and repeatable access to a trusted method at lower cost than repeated one-to-one service.
Commercial forms

    monthly or annual subscription;

    usage credits;

    team subscription;

    bundled membership or course;

    API/Agent access;

    platform revenue share later.

A creator-published SaaS is not another foundational family. It is Job A plus Job B at a more advanced distribution position.
3.3 Job C — Improve candidates and methods against evidence
Business statement

    I want humans and Agents to use proprietary and public evidence to generate alternatives, compare them, observe what actually happens, and improve both the solution and the method that produced it.

Representative domains

    industrial formulation;

    portfolio construction;

    investment research;

    macro forecasting;

    factory scheduling;

    supplier allocation;

    SME go-to-market;

    marketing and creator growth;

    clinic operations;

    education planning;

    energy and logistics;

    scientific research;

    organizational or city planning.

What the user can do

The user can:

    connect proprietary and public Sources;

    describe a problem, objective, hypothesis, or desired result;

    express hard constraints, units, tolerances, and soft preferences;

    generate and retain several candidate Artifacts;

    run deterministic calculations, simulations, or constraint checks;

    compare candidates with an incumbent;

    review assumptions, confidence, and uncertainty;

    approve, reject, edit, defer, or attest a result;

    execute or test a candidate under controlled authority;

    capture delayed real-world outcomes;

    distinguish preference signals from actual performance;

    compare Agent or method revisions under controlled conditions;

    promote or roll back a revision without deleting the incumbent.

Generic loop

Evidence and context
      ↓
Problem / objective / hypothesis
      ↓
Agent exploration
      ↓
Candidate Artifact
      ↓
Deterministic checks / simulation / expert review
      ↓
Decision
      ↓
Test, deployment, or observation
      ↓
Delayed real-world Outcome
      ↓
Comparison with expectation and incumbent
      ↓
Improved candidate or method

Business value

    lower cost;

    higher yield;

    improved forecast accuracy;

    improved risk-adjusted decisions;

    faster R&D;

    more alternatives tested;

    better use of expert time;

    safer, reproducible decision processes.

Commercial forms

    paid pilot;

    professional or enterprise subscription;

    private deployment;

    research partnership;

    compute or usage pricing;

    selected value-based contracts.

3.4 Cross-cutting consumption modes — channel-first Agent Job Delivery and external/headless use

This is not a fourth business job. It is how Jobs A, B, or C may be requested, steered, approved, and delivered when the user is not primarily operating through a full Boring application.
3.4.1 Agent Job Delivery
Business statement

    I want to submit a bounded job with inputs, constraints, expected deliverables, budget, deadline, and allowed effects; let an authorized Agent complete the Work asynchronously; answer questions or approvals only when needed; and receive durable, versioned Artifacts with provenance.

Representative jobs include:

research these companies
→ investment memo

analyze this dataset
→ report and dashboard

review this contract
→ issue list and revised draft

prepare this campaign
→ content package

compare these suppliers
→ sourcing recommendation

turn these notes into a deck
→ presentation

The customer may buy the job without initially wanting a full persistent application. The richer application can emerge later around history, connected Sources, editing, dashboards, recurring jobs, automation, team access, and outcomes.
3.4.2 Channel-first intake, steering, and delivery

The first interaction may occur through:

    a first-party Boring chat or composer;

    WhatsApp, Telegram, Slack, Teams, email, SMS, or voice;

    an embedded widget inside an existing application;

    MCP or another Agent-facing protocol;

    HTTP API or webhook.

The channel is an intake, steering, approval, notification, and delivery surface. It is not the durable identity of the Work.

The common lifecycle is:

brief / input
→ identity and entitlement resolution
→ clarification and proposed Job Contract
→ durable Work admission
→ Agent execution
→ questions or approvals when required
→ durable Artifact delivery
→ accept, revise, reject, or close
→ optional later Outcome

Not every message should automatically become a job. Ambiguous requests may remain conversational until the Agent and user agree on the expected result, inputs, constraints, cost, and effects.

The same Work may move between channels:

WhatsApp intake
→ secure Source connection in the Boring app
→ approval in the attention inbox
→ final PDF by email

Input, progress, approval, and delivery channels may differ. The platform must preserve one Work identity across them.

Channel policy may intentionally restrict what can happen. A messaging channel may accept a brief, show status, ask questions, and deliver secure links while refusing to expose confidential Source content, credentials, or high-risk approval controls.
3.4.3 External and headless machine consumption
Business statement

    I want an authorized external Agent, MCP client, service, or existing application to submit structured Work to a Boring product and receive a durable result without receiving direct authority over private customer data.

Representative shapes:

    a foreign Agent calls a research or analysis capability;

    an existing CRM requests a governed Agent result;

    an MCP client submits a job and receives an Artifact or resource;

    an external client raises a human approval in Boring only when needed;

    a headless expert Agent returns a report, draft, decision support result, or attestation.

3.4.4 Required channel-neutral business capabilities

    external identity binding and entitlement resolution;

    conversation-to-Work linking without making the conversation the Work identity;

    message and request idempotency;

    secure attachment import and content-origin labeling;

    purpose-scoped input Projection;

    structured Job Contract and admission;

    progress, blockers, questions, and terminal status;

    approval choices tied to the exact Work, Operation, input, and Artifact revision;

    durable Artifact output and delivery receipt;

    cross-channel continuity;

    channel-specific data and effect policy;

    metering and provenance by consumer, channel, Work, and Artifact;

    no assumption that a Page, chat transcript, or workbench is required to invoke the domain behavior.

The full SaaS Experience may be the persistent control plane, while chat, WhatsApp, MCP, email, or another channel is the first point of value delivery.
3.5 Cross-cutting internal mode — Boring as its own operator, code factory, and improvement customer

Boring itself should be a valid customer of the platform.

Boring already uses a factory pattern in which an orchestrator coordinates worker Agents, automated review gates check their output, and items requiring human judgment are posted into an inbox. This pattern should be treated as a first-party reference use case for the platform—not as a privileged subsystem with separate semantics.
Existing and intended factory loop

backlog item / product request / defect
→ orchestrator admits and decomposes Work
→ worker Agents receive bounded child Work, Sources, budgets, and isolated execution
→ workers produce code patches, tests, reports, or Package/Experience revisions as Artifacts
→ automated gates run type checks, tests, security checks, evaluation packs, and policy checks
→ unresolved risks, diffs, and review questions become Attention Items
→ human reviews in the inbox
→ approve, request revision, reject, merge, release, or deploy
→ production and commercial evidence returns to the next cycle

The internal code factory may use the same capabilities to:

    inspect commercial, product, support, and repository signals;

    create product, architecture, Agent, Package, and Experience candidates;

    decompose Work across orchestrator and worker Agents;

    isolate workers by branch, worktree, sandbox, Source Projection, authority, and budget;

    generate code or declarative revisions;

    run deterministic tests, CI, benchmarks, and evaluations;

    compare candidate and incumbent behavior;

    post review-gated diffs and evidence to the same human attention inbox used by customer products;

    allocate bounded research, development, or marketing budgets;

    promote, merge, release, deploy, or reject revisions only through authorized gates;

    retain cost, latency, failure, review, deployment, and outcome evidence.

Factory invariants

    the orchestrator may allocate and narrow Work but may not bypass policy;

    worker Agents may propose changes but may not merge, publish, deploy, or widen their own authority;

    automated review gates are versioned evaluators, not unquestioned truth;

    human approval is attached to the exact diff, Artifact, test state, and release candidate;

    a failed worker or gate does not corrupt the parent Work;

    the same Work, Execution, Artifact, Attention, Evidence, and promotion semantics apply to internal and customer-facing products.

This is not a fourth architecture or a special self-modification bypass.

The internal operator is simply another Instance owner using the same Work, Operation, Artifact, Evidence, evaluation, and revision pathways.
3.6 Cross-cutting developer distribution mode — build locally, host on Boring

This is not a fourth business job. It is a distribution and platform-consumption path for developers who want to build Agent products with Boring’s framework and optionally run them on Boring’s hosted infrastructure.
Business statement

    I want to create an Agent or agent-native application with a normal developer workflow, run and evaluate it locally, then deploy it to Boring without rebuilding authentication, Agent runtime, Sources, governance, observability, hosting, or commercial infrastructure.

A canonical developer journey may look like:

npx boring create my-agent
cd my-agent
npx boring dev
npx boring test
npx boring deploy

Possible templates may include:

    bounded job-delivery Agent;

    chat-first expert Agent;

    route-first domain SaaS;

    headless MCP/API Agent;

    background worker or automation;

    data-rich Agent with semantic queries;

    package or extension for an existing Boring application.

The developer can potentially:

    define Agents, Operations, Sources, Experience, and evaluation fixtures in versioned manifests;

    use local and hosted environments through the same durable contracts;

    bind local test Sources without embedding production secrets;

    preview requested capabilities and trust boundaries;

    deploy an immutable release;

    configure hosted Sources, secrets, domains, regions, budgets, and entitlements;

    inspect Work, traces, costs, failures, and Artifacts;

    roll back or promote a previous release;

    choose Boring-hosted, dedicated, or later self-hosted deployment paths;

    publish or sell the resulting Agent/product later without changing its runtime identity.

The deployed application should become an ordinary Package/Instance/Agent product, not a special “developer mode” runtime.

Possible commercial forms include:

    free or open-source local framework usage;

    hosted developer subscription;

    platform fee plus usage;

    per-Instance or per-environment hosting;

    enterprise dedicated deployment;

    later publisher revenue sharing or marketplace distribution.

The developer path is strategically important because it distributes the platform through builders, not only through Boring-authored verticals or creator audiences.
3.7 How the jobs combine
Example	Operate	Distribute	Improve	External/headless
Creator Studio	High	Possible later	Outcome-aware	Optional
Creator’s Personal Agent	Medium	High	Personalized / package-level	Common
Creator-Published SaaS	High	High	Optional to high	Optional
Macro Research Terminal	High	Possible	High	Common
Investment Workbench	High	Possible	High	Optional
Industrial Formulation	High	Possible	High	Optional
SME Pipeline Tool	High	Possible	Medium to high	Common
Channel-First Agent Job Service	Medium	High	Optional to high	Essential
Developer-Hosted Agent/App	High	High	Optional to high	Common
Boring Internal Code Factory	High	Internal package reuse	High	High
4. Independent evolution dimensions

The jobs above can evolve independently. These dimensions are not separate platforms.
4.1 Distribution

private application
→ local developer project
→ Boring-hosted deployment
→ reusable internal Package
→ installed product
→ expert subscription
→ public product
→ Agent/API service

Capabilities may include:

    developer scaffolding, local development, testing, and deployment;

    immutable Package and release versions;

    hosted Instance creation, environment configuration, logs, and rollback;

    publisher/developer identity and lineage;

    isolated Instances;

    local overlays;

    upgrade and rollback;

    entitlements;

    private, unlisted, and public offerings;

    publisher economics;

    machine-facing access.

4.2 Adaptivity

human-only
→ Agent-assisted
→ personalized
→ evidence-aware
→ outcome-driven
→ evaluated and recursively improved

The application’s domain identity does not change as it moves right.
4.3 Locus of initiative

human-initiated
→ Agent-proposed
→ event-initiated
→ scheduled
→ queue/factory-pulled

Examples:

    a user asks from a Page;

    the Agent proposes a stale-opportunity follow-up;

    a new mail message creates Work;

    a data release reopens a forecast comparison;

    a lab result queues the next formulation candidate;

    a factory puller claims the next ready Work item.

Every non-human initiation must carry authority, budget, provenance, and a destination in the attention plane.
4.4 Per-operation autonomy

Autonomy is not a global Agent setting.

It is a policy for an Operation, Actor, and context:

observe
→ suggest
→ draft
→ act with approval
→ act within policy
→ act with post-hoc review

A product may gradually increase autonomy for one operation while keeping another permanently human-gated.

Changing the autonomy level is itself a governed revision supported by evidence and revocation.
4.5 Agent cardinality

one Agent
→ specialized Agents
→ delegated Agent team
→ authorized external Agents

Capabilities may include:

    stable identity;

    roles and bindings;

    routing;

    bounded delegation;

    different authority ceilings;

    budgets;

    parent/child Work and execution provenance;

    optional debug visibility.

The user should normally see Work and results, not a swarm of chat windows.
4.6 Human collaboration

one user
→ team
→ organization
→ controlled external collaborator

Capabilities may include:

    membership;

    roles;

    shared Work and Artifacts;

    comments and assignments;

    approvals;

    activity;

    personal presentation state;

    audit;

    controlled sharing.

4.7 Application composition

one application
→ several modules
→ several installed products
→ composed personal or team environment

The business option requires:

    namespaced contributions;

    stable Resource references;

    package and entitlement boundaries;

    cross-application links;

    possible shared search and attention;

    optional navigation composition.

It does not require Part 2 to assume that one Work object must span several commercial products. Cross-application references and a shared attention plane may be sufficient. Unified cross-app Work remains an open product decision.
4.8 Time and causality

immediate preference
→ delayed Outcome
→ vintage-sensitive Outcome
→ noisy or contested Outcome
→ never-observed Outcome

Capabilities may include:

    accept/edit/reject now;

    link later measurements to the originating execution and Artifact;

    freeze data vintage;

    record observation windows;

    preserve confounders and attribution confidence;

    record missing outcomes honestly;

    prevent missing data from being interpreted as success.

4.9 Deployment and sovereignty

local
→ Swiss-hosted
→ EU-hosted
→ dedicated environment
→ customer-controlled deployment

The application, Agent, Work, Artifact, and Package identities should survive changes in:

    model provider;

    sandbox or compute provider;

    host process;

    storage implementation;

    region;

    deployment topology.

4.10 Output and liability class

Not every result carries the same business or legal meaning.
Class	Meaning	Product rule
Advice	Interpretation, recommendation, or draft. A human or professional remains responsible.	Clearly labeled; must not silently become an Action.
Action	A change to owned or external state.	Requires authority, effect handling, idempotency or reconciliation, and proof.
Attestation	A released or signed claim the Instance owner or publisher is willing to stand behind.	Versioned, attributable, and backed by deterministic calculation and/or authorized human sign-off.

This distinction lets the same platform support a creator tool and a regulated research or operations product without applying identical ceremony to both.
4.11 Commercial and billing model

free / internal
→ subscription
→ seat or Instance
→ per job or deliverable
→ usage / credits
→ retainer or managed service
→ publisher subscription / revenue share
→ outcome-linked or shared-value contract where attribution permits

Commercial structure is an independent product dimension. The platform should not assume that every vertical is billed per token, per seat, or through one marketplace model.

The business model may vary by:

    payer: subscriber, Instance owner, publisher, enterprise contract, external consumer, or platform subsidy;

    unit sold: access, seat, Instance, Work/job, Artifact/deliverable, usage, compute, service level, or measured Outcome;

    cost bearer: platform, publisher, subscriber through bring-your-own-provider credentials, or a negotiated combination;

    commercial relationship: self-service subscription, prepaid credits, monthly retainer, paid pilot, annual enterprise contract, developer hosting, revenue share, or value-based agreement;

    settlement: direct invoice, payment-provider collection, publisher payout, internal cost center, or no external payment.

The platform should distinguish five layers:

Usage facts
→ what resources and attention were consumed

Entitlement and budget
→ what the customer may use and the ceilings that apply

Pricing policy
→ how the offer maps value or usage to a charge

Billing and collection
→ invoice, payment, tax, credit, or contract administration

Settlement and payout
→ how revenue and cost are allocated among platform, publisher, developer, and provider

Usage facts and attribution must remain stable even when the pricing model changes. Pricing, billing providers, taxes, invoices, and payouts should remain configurable commercial policies.

Outcome-linked pricing is appropriate only when the Outcome and attribution are sufficiently credible. The system must never invent certainty merely to support a billing model.
5. Required business capabilities

This section describes the capability space. It does not decide implementation order.
5.1 Purpose-built Experience capabilities

The same platform must support:

normal route-first SaaS
minimal expert-Agent product
research workbench
chat-first product
headless product

Required capabilities may include:

    domain-owned navigation;

    routed Pages;

    reusable Views;

    tables, records, forms, charts, documents, editors, timelines, maps, and dashboards;

    optional workbench hosting with tabs and panes;

    chat as an optional View over Work;

    ambient Agent presence;

    Agent drawer or dedicated Agent page;

    semantic deep links into exact records, Artifacts, or Work;

    responsive and multilingual presentation;

    branding and themes;

    personal View state separate from shared Work;

    deterministic rendering without a model call;

    graceful Level 0 operation.

Agent presence modes

Products may choose:

hidden
ambient
drawer
page
roster

ambient is often the natural SaaS mode:

    suggestions;

    background completions;

    attention items;

    inline actions;

    small composer;

    approval cards.

No presence mode changes the domain operation model.
Navigation and presentation rules

    stable product destinations belong in navigation;

    active Work objects may appear as tabs where useful;

    tabs and panes are presentation state, not business identity;

    a file tree is a navigator into a Source, not the document View itself;

    an Agent opens a semantic target or View intent, not a concrete React component or Dockview panel ID.

5.2 Attention plane and human-control channels

The human’s universal control surface for asynchronous Agent work is not necessarily chat. It is the bounded queue of things requiring judgment.

The attention plane may contain:

    approvals;

    questions;

    reviews;

    exceptions;

    completed background Work;

    unknown external effects;

    outcome confirmations;

    escalations;

    reminders.

Required capabilities:

    one attention item links to the exact Work, Artifact, record, Operation, or revision;

    accept, edit, reject, defer, delegate, or reassign;

    batch review for repetitive low-risk items;

    propose a policy/autonomy change instead of endlessly repeating approvals;

    deadlines and escalation;

    ranked attention by urgency, value, risk, and human budget;

    personal channel preferences;

    answer from in-app UI, email, chat, mobile notification, or another approved channel;

    audit who responded, where, and under which identity;

    preserve the same approval semantics across products and channels.

Attention budget

A useful product promise may be:

    This system will consume no more than N human decisions per day unless a declared incident occurs.

Human attention should be measurable alongside model and compute cost.
5.3 Durable Work, continuity, recoverability, and service quality

Work must survive:

    browser closure;

    device change;

    model-provider outage;

    Agent process death;

    sandbox or compute expiration;

    network interruption;

    duplicate submission;

    partial operation success;

    uncertain external effects.

Required capabilities:

    stable Work identity;

    resumable history and current status;

    linked participants, Sources, records, and Artifacts;

    queued, admitted, provisioning, running, waiting-for-human, blocked, completed, failed, cancelled, rolled-back, and outcome-unknown states where appropriate;

    reconnect and snapshot/replay behavior;

    a stable execution identity for every non-trivial admitted unit of Work;

    idempotency keys for consequential Operations;

    completion proof or explicit unknown state;

    compensating or follow-up action rather than silent retry of uncertain effects;

    preservation of costs already incurred;

    asynchronous execution by default for long-running work;

    separate interactive and background latency budgets;

    support for SLA classes where a product promises response or completion windows;

    history and dashboard access without leasing an Agent runtime.

A product that drafts brilliantly and cannot recover from a mid-flight failure is a demo, not software.
Bounded Agent Job Delivery

A bounded job should support:

    structured or conversational intake;

    required-input validation;

    clarification before admission when the brief is ambiguous;

    an explicit deliverable contract;

    budget, deadline, and service-level constraints;

    progress and blocker states;

    questions and approvals that pause and resume the same Work;

    partial delivery where the contract allows it;

    versioned Artifact delivery;

    acceptance, revision request, rejection, cancellation, and closure;

    delivery receipt and notification;

    optional later Outcome association.

A job may be initiated and delivered through different channels. External message and conversation IDs must link to Work rather than replace its identity.
Work economics

Every significant Work item and Artifact should be able to carry:

    model/provider usage;

    tool or deterministic-kernel cost;

    sandbox or compute time;

    external API cost;

    human attention time where known;

    success, failure, or unknown status;

    Actor and Agent revision;

    originating channel;

    value or outcome references.

These are usage facts, not invoices.

They enable later pricing, margins, budgets, and optimization of result per euro and human minute.
5.4 Actor, Agent, memory, and identity capabilities

Required capabilities:

    stable human, Agent, automation, service, and external-client identity;

    immutable Agent behavior revisions;

    explicit binding of an Agent into an Instance or product context;

    role, capability ceiling, budget, and model policy per binding;

    current semantic context projection;

    structured outputs;

    human approval requests;

    parent/child execution provenance for delegation;

    per-Agent and per-Work budgets;

    revocation;

    cost attribution;

    optional subscriber-local personalization;

    optional multi-Agent routing.

Agent identity invariant

An Agent’s stable identity and approved definition lineage remain independent of a particular application or deployment.

Applications and subscribers may constrain or personalize the Agent through bindings and scoped overlays. They do not silently mutate the global Agent identity.

A fork or derived expert Agent may create a new lineage explicitly.
Memory scope

Memory must declare scope:

user-local
Instance-local
organization-local
publisher/package-level
platform-level

A memory written at one scope never becomes visible at a wider scope without an explicit, audited promotion or export.

Memory is not evidence. Evidence is not automatically retrieval knowledge.
5.5 Domain contract, Operation parity, and deterministic kernels
Domain contract

Humans and Agents should speak the application’s nouns and verbs:

Account
Opportunity
Ingredient
Forecast
Formulation
Campaign
Move to Negotiation
Record lab result
Draft follow-up

not provider paths, plugin IDs, raw table names, or UI component names.

The Domain Contract should make available:

    discoverable entities and identifiers;

    typed Operations and schemas;

    effect and reversibility class;

    approval and autonomy policy;

    applicable constraints;

    current-context projection;

    progressive discovery by Work and authority;

    lower-level governed escape hatches where necessary.

Operation parity

If a human can perform a meaningful domain action, it must be structurally Agent-addressable.

This is one-directional:

    not every Agent-only exploration needs a human button;

    not every UI gesture is an Operation;

    presentation state can remain human-only;

    the Agent can use longer time horizons or controlled fan-out subject to budgets.

The Agent should receive only the Operations relevant to current Work and authority, rather than the full catalog in every prompt.
Effect taxonomy

Every Operation declares an effect class.
Effect class	Meaning
observe	Reads governed state. Safe to repeat subject to rate and privacy limits.
compute / simulate	Pure deterministic calculation or simulation with no live-state change.
propose	Creates a draft, candidate, comparison, or Artifact without activating it.
mutate	Changes application-owned state. Must declare reversibility or compensation.
external-effect	Crosses a boundary the platform does not control: send, publish, order, pay, file, deploy. May end in outcome-unknown and must not be blindly retried.

Approval, retry, automation eligibility, audit depth, and autonomy policy derive from this classification.
Reversibility and dry-run

Operations may declare:

reversible
compensable
irreversible

Where applicable, they should support:

    dry-run or plan-preview;

    intended-effects display;

    compensating Operation;

    optimistic concurrency or version precondition;

    explicit conflict when a human and Agent change the same state;

    completion receipt.

Deterministic domain kernel

Where the buyer is purchasing a number, feasibility decision, or regulated calculation, the LLM must not be the calculator of record.

Calculation-heavy products need a non-LLM kernel with:

    versioned functions, rules, solvers, or simulations;

    explicit units and schemas;

    fixtures and golden tests;

    reproducible outputs;

    explanation of calculation provenance;

    shared use by UI, Agent, evaluator, and outcome comparison.

Examples:

    portfolio weights, risk, concentration, and liquidity;

    nutrition, cost, and formulation constraints;

    time-series transformations and vintage alignment;

    pricing, tax, and scheduling feasibility;

    clinic or operational rules.

The Agent may search, propose, and interpret.

The deterministic kernel computes and rejects infeasible candidates.
5.6 Source, authority, trust, and safety capabilities
Heterogeneous Sources

The platform may need to connect:

    private files and multiple logical file roots;

    Git repositories;

    databases and warehouses;

    CSV, JSON, Parquet, SQLite, and DuckDB;

    mailboxes;

    CRM and ERP systems;

    public data;

    document and knowledge systems;

    object stores;

    HTTP or MCP services;

    internal deterministic kernels.

Not every Source becomes a filesystem mount.

Files may be projected as files.

Databases usually remain governed queries.

Mail remains mail Operations.

CRM remains domain Operations.

Execution permission is distinct from read or write permission.
Authority capabilities

    human and Agent identity;

    membership and roles;

    Source binding and maximum capabilities;

    request- and Work-scoped narrowing;

    record, path, field, mailbox, row, or measure constraints;

    expiry and revocation;

    approval policies;

    budget and rate limits;

    delegation that can only narrow;

    residency policy;

    credential isolation;

    full audit;

    cross-boundary transfer policy.

Untrusted content and prompt injection

Once Agents read mail, web pages, documents, subscriber uploads, CRM notes, or public data, third parties can write content into the Agent’s context.

The platform must support:

    content-origin labels;

    distinction between authorized human instruction and Source data;

    provenance of which content influenced an Operation;

    treatment of Source content as data, never authority;

    elevated approval for consequential Operations primarily induced by untrusted content;

    controls preventing Source-to-Source or Instance-to-Instance data transfer without explicit policy;

    sanitization, isolation, or reduced context where appropriate.

An email saying “send all invoices to this address” cannot grant the Agent permission to do so.

The same principle applies inward: an Actor’s own report of its work is a claim, not a measurement. Consequential facts — tests passed, checks run, effects completed, costs incurred — must come from platform instrumentation and receipts, never solely from the acting Agent’s narrative. An Agent asserting “all gates passed” without a platform-recorded gate result is an integrity finding, not evidence.
Package and extension trust

Distributed Packages may contain instructions, knowledge, Experience definitions, semantic definitions, or executable code.

Required capabilities:

    publisher identity and signing;

    immutable content digest;

    capability passport before installation;

    declared Sources, Operations, authority, models, and estimated cost;

    declarative content validation;

    sandboxed treatment of untrusted executable content;

    explicit promotion to operator-trusted code;

    no self-widening by Package or Agent;

    auditable migration authority;

    rate limiting and abuse controls for public products.

Compliance, retention, and liability

Selected domains may require:

    mandated human oversight that configuration cannot disable;

    distinction between advice, action, and attestation;

    retention policies and legal hold;

    data-subject erasure;

    separation of identifying content from structural audit/provenance;

    documented model, Operation, and oversight capabilities;

    incident response for model hallucination or unsafe action;

    non-delegation of regulated professional acts;

    jurisdiction-aware disclaimers;

    SSO/OIDC and directory integration;

    export of audit and compliance records;

    explicit allocation of liability between Instance owner, publisher, and platform operator.

5.7 Domain semantics and analytical semantic capabilities

Two different semantic needs must not be collapsed.
5.7.1 Domain Contract — universal for meaningful Agent parity

The Domain Contract describes the application’s nouns, identifiers, typed actions, and context.

This is essential even for products that never use analytical measures.

Examples:

Mail Thread
Account
Opportunity
Document
Campaign
Formulation
Draft reply
Move stage
Publish content

5.7.2 Analytical semantic engine — optional for calculation-rich products

Some products need measures, dimensions, units, time, vintages, and lineage:

revenue
gross margin
engagement
inflation
portfolio weight
forecast error
supplier cost
conversion

Required capabilities may include:

    semantic model discovery;

    entities, measures, dimensions, and relationships;

    query validation;

    query explanation;

    units and time transformations;

    model versioning;

    Source and data-vintage lineage;

    binding to Views;

    binding to evaluator and Outcome definitions;

    governed SQL or lower-level escape hatch.

BSL’s role

BSL can be the preferred implementation for data-rich products behind stable semantic Operations such as:

semantic.describe
semantic.query
semantic.explain

BSL should not become the universal application model.

Mail is not a cube. A document editor is not a cube.

The stable business contract is semantic discovery, query, explanation, and provenance—not Python as an implementation language.
Semantic provenance

Every semantic result used as evidence should retain:

    semantic model identity and digest;

    query or expression;

    generated plan where available;

    underlying Source identities;

    data snapshot or vintage;

    result digest;

    limits and truncation;

    executing identity and authority digest;

    linkage to Work, Artifact, evaluator, and Outcome.

5.8 Artifacts, evidence, outcomes, and the join requirement
Artifact capabilities

Artifacts may include:

report
draft
email
spreadsheet
chart
dataset
formulation
portfolio candidate
forecast
presentation
View definition
Agent revision
package revision

Required capabilities:

    stable identity;

    immutable or versioned content;

    type;

    producing Actor and execution;

    input and Source references;

    citations;

    approval and release status;

    comparison and supersession;

    deep link into the correct View;

    export and sharing;

    relationship to later outcomes.

Not every message is an Artifact.

Not every Artifact is a candidate.
Single durable evidence join

Every admitted unit of Agent or automation Work must have one durable identity that joins:

    request and context;

    Actor and Agent revision;

    authority and policy revision;

    Source and semantic model versions;

    executions and retries;

    Operations and effects;

    costs, latency, and failures;

    Artifacts;

    human decisions and edits;

    delayed real-world outcomes;

    evaluations and promotion decisions.

Evidence that cannot be joined back to the Work that produced it is decoration.
Preference versus Outcome

The platform must distinguish:

human accepted it
human edited it
human rejected it

from:

it received a reply
it improved conversion
it reduced formulation cost
it predicted the data accurately
it produced a production incident

Both matter. They are not interchangeable.
Delayed, noisy, and missing outcomes

Required capabilities:

    link outcomes days, weeks, or months later;

    define observation window;

    preserve data vintage;

    preserve relevant confounders;

    attach attribution confidence;

    support controlled, before/after, holdout, or observational comparison designs;

    record partial and contested outcomes;

    record “not observed” honestly;

    avoid treating missing outcomes as success;

    keep evidence scope and permitted purpose explicit.

5.9 Evaluation, incumbent comparison, and controlled promotion

The architecture-preserving capability is an evaluation substrate.

Automatic self-improvement is one possible use of it.

The sellable near-term concept is often simpler:

    compare two drafts;

    compare two formulations;

    compare two forecasts;

    compare two Agent revisions;

    keep the loser;

    know why the winner won;

    revert when later outcomes disagree.

A product supports controlled improvement when it can:

    identify the target;

    define the incumbent;

    define “better” and guardrails;

    define the comparison design;

    gather sufficient, comparable evidence;

    propose an immutable revision;

    validate it;

    evaluate it using a versioned evaluator;

    retain rejected candidates, their evaluations, and rejection reasons as queryable evidence, so future proposals can condition on the full candidate history rather than rediscovering failures;

    keep candidate generation, evaluation, promotion, and production execution under distinct authorities;

    require authorized promotion;

    activate through pointer movement or equivalent version selection;

    monitor and roll back.

Evaluation validity

Capabilities must address:

    held-out or prospective evaluation;

    comparable conditions and data vintages;

    minimum sample or evidence threshold;

    confidence intervals or equivalent uncertainty reporting where appropriate;

    slice regressions and guardrails;

    evaluator versioning;

    Goodhart and proxy gaming;

    budget-matched comparison: a candidate beats an incumbent only at equal cost, latency, and human-attention budget, or the difference is declared;

    promotion-rate observability: the ratio of proposed to promoted revisions is recorded; a gate that accepts most candidates signals a weak evaluator or negotiated criteria, not a strong pipeline;

    cost, latency, and safety, not only quality;

    evaluation leakage;

    repeated measurement;

    human expert panels where numerical ground truth is unavailable.

An evaluator must not be silently changed in the same promotion as the object it judges.

Human-authored and Agent-proposed changes use the same revision pipeline. Authorship is metadata, not an exemption.
Evaluation integrity against optimizing Actors

Any Actor that is repeatedly measured will eventually optimize the measurement rather than the goal. Empirically, self-improving systems attack their own fitness signal before they attack anything else.

The platform must therefore support:

    held-out evaluation evidence that the judged Actor cannot read, condition on, or influence — distinct from the signals visible to it during Work;

    outlier discipline: an implausibly large improvement triggers an audit obligation, not an automatic promotion;

    gaming detection as evidence: weakened assertions, disabled checks, deleted instrumentation, and metric-satisfying-but-intent-violating changes are recordable findings linked to the Execution and revision;

    separation of duty: the Actor being judged may not author, select, modify, or schedule the evaluator, fixtures, or held-out evidence that judge it, in the same or a linked promotion;

    refresh obligations: fixtures and held-out sets are consumable — repeated exposure degrades them — and their rotation is a versioned, audited act.

These are integrity capabilities, not optimization features. They apply to the internal code factory exactly as to customer products.
5.10 Automation, events, schedules, and attention channels

Meaningful Work may begin without a user typing.

Trigger capabilities may include:

    schedules;

    domain events from Operations;

    Source change detection;

    new mail or files;

    data releases;

    inbound webhooks and APIs;

    threshold or exception detection;

    queue/factory pull.

Automations must behave as governed Actors with:

    identity;

    owner;

    authority ceiling;

    budget;

    rate limits;

    effect-class restrictions;

    provenance;

    pause, dry-run, and disable;

    loop detection and circuit breakers.

An automation chain must not create an uncontrolled financial or operational loop.
Channel-first intake and delivery

Inbound and outbound channel capabilities may include:

    first-party chat or composer;

    WhatsApp, Telegram, Slack, Teams, email, SMS, or voice;

    embedded widgets;

    MCP and Agent-facing protocols;

    HTTP API, webhook, and callback.

Channel adapters should support:

    external identity and subscription/entitlement binding;

    message idempotency and ordering tolerance;

    conversation-to-Work linking;

    secure attachment import;

    content-origin and trust labeling;

    channel-specific data and effect policy;

    clarification and Job Contract confirmation;

    progress projection and notification;

    questions and approvals bound to the exact Work and Operation;

    secure Artifact delivery or deep links;

    delivery receipts;

    cross-channel continuation.

Attention and delivery channels may include:

    in-app inbox;

    email;

    WhatsApp or team chat;

    mobile push;

    digest;

    external client callback;

    MCP resource/result delivery.

The same approval record, Work identity, and Artifact lineage must survive across channels.
5.11 Agent-built and Agent-customizable applications

A user may ask:

“Add a supplier-risk page.”
“Show sponsor revenue beside each campaign.”
“Create a forecast comparison dashboard.”
“Add an approval step.”
“Make LinkedIn the primary channel.”

The Agent needs three reflective catalogs.
Operation Catalog — what can happen

    domain actions and queries;

    schemas;

    effects and reversibility;

    approval and autonomy policy;

    constraints;

    result shapes.

Semantic Catalog — what structured data means

    entities;

    measures;

    dimensions;

    relationships;

    units;

    filters;

    vintages and lineage.

Experience Catalog — how humans can see and control it

    View types;

    data-binding contracts;

    action-binding contracts;

    layout rules;

    accessibility requirements;

    supported presentation modes.

Three mutation lanes

    Instance parameters and overlays
    Filters, defaults, local branding, arrangement, and scoped preferences.

    Catalog-constrained declarative changes
    Pages, Views, navigation, calculations, semantic bindings, action bindings, and workflow configuration represented as validated data.

    Trusted code Package revisions
    Built in isolation, checked, tested, reviewed or policy-approved, versioned, and rollbackable.

The Agent may propose declarative changes.

The Instance owner may approve local overlays.

Only a trusted promotion path may add executable code.

No Actor may approve a change that widens its own authority or swaps in an evaluator that judges the same change.
Customization flow

User intent
→ inspect catalogs and current Experience
→ propose revision
→ validate schemas, references, effects, and accessibility
→ dry-run / preview
→ show diff and capability changes
→ human or policy approval
→ activate versioned overlay or Package revision
→ monitor and roll back

If the request cannot be built from approved catalogs, the correct product behavior is a refusal plus a human package-engineering path—not arbitrary production code injection.
5.12 Packaging, installation, distribution, and upgrade safety

A reusable Agent or application may need:

    stable Package identity;

    immutable version and digest;

    publisher identity and signature;

    methodology, knowledge, skills, and Agent definitions;

    Experience definition;

    semantic definitions;

    deterministic kernels and fixtures;

    Source requirements;

    Operation and authority requirements;

    capability passport;

    cost estimate;

    migrations and compatibility metadata;

    installation;

    isolated Instance creation;

    local overlays;

    upgrade and rollback;

    export/import;

    share versus install distinction;

    fork/derive lineage;

    human and Agent-facing access;

    usage metering and entitlement;

    publisher analytics within the permitted boundary.

Capability passport

Before installation or subscription, the user should be able to understand:

what Sources the Package requests
what Operations it can call
which actions require approval
whether executable code is present
which models or regions it expects
estimated usage and cost
what evidence can leave the Instance

Upgrade safety

A publisher upgrade meets:

new Package version
+
subscriber local overlays
+
subscriber private data

Required capabilities:

    never silently discard overlays;

    surface conflicts for decision;

    dry-run migrations against a copy or checkpoint;

    version and audit migrations;

    run migrations with bounded authority and no publisher read-back channel;

    keep the incumbent intact after failed or rejected upgrades;

    ship evaluation fixtures and change information with a release;

    let subscribers defer or reject an upgrade.

5.13 Multi-Agent, multi-user, and multi-application composition
Multi-Agent

Capabilities may include:

    several Agent bindings;

    role and capability differences;

    routing;

    bounded delegation;

    parent/child Work;

    structured return Artifacts;

    review and provenance;

    separate budgets;

    user-visible status without mandatory Agent-to-Agent chat.

Multi-user

Capabilities may include:

    membership and roles;

    shared Work and Artifacts;

    comments, assignments, approvals, and activity;

    organization identity;

    controlled external sharing;

    personal presentation state;

    shared recommended context.

Multi-application

Capabilities may include:

    several installed Packages;

    namespaced contributions;

    preserved entitlements and lineage;

    cross-application Source or Artifact references;

    shared search or attention;

    optional navigation composition;

    optional global composer.

Business principle:

    Adding an Agent, collaborator, or Package should add a binding or contribution rather than require another authority, Work, or operation architecture.

The exact UX of a universal multi-app shell and unified cross-app Work remains intentionally unresolved.
5.14 Cloud, sovereignty, compliance, and degraded operation

The platform may need to support:

    local execution;

    Swiss or EU hosting;

    dedicated tenancy;

    customer-controlled deployment;

    replaceable model providers;

    replaceable compute and sandbox providers;

    customer-held credentials;

    encrypted secrets;

    private networking;

    regional storage and execution policy;

    backup, restore, export, and deletion;

    tenant isolation;

    audit and diagnostics;

    provider outage degradation;

    read-only access to prior Work and Artifacts when the model is down.

Sovereignty means more than data location.

It also means the customer retains:

    identity and membership;

    application data;

    Source configuration and credential references;

    Work history;

    Artifacts;

    human decisions;

    evidence and outcomes;

    Package and revision lineage;

    the ability to export or continue operating in degraded mode.

5.15 Developer, publisher, and operator capabilities

The people building and operating applications—including Boring’s own team—need to understand and repair Agent behavior in minutes, not days.

Required capabilities may include:

    per-execution trace of context, model calls, Operations, effects, cost, latency, decisions, and errors;

    replay against a new Agent, model policy, evaluator, or semantic revision;

    local development with the same Work and Artifact contracts as hosted execution;

    evaluation fixtures runnable locally, in CI, and during Package publication;

    diagnostics by Instance, Agent, Source, and Package without violating customer privacy;

    package compatibility and migration tests;

    cost and error dashboards;

    incident response and reconciliation tools;

    kill switches and budget freezes;

    support-safe redaction;

    immutable release and rollback tools;

    a developer CLI and SDK supporting flows such as npx boring create, npx boring dev, npx boring test, and npx boring deploy;

    templates for job Agents, expert Agents, SaaS applications, headless Agents, and workers;

    local/hosted contract parity for Work, Operations, Sources, Artifacts, evaluation, and policy;

    deploy previews, capability passports, secret/Source binding, region selection, logs, health, rollback, and release promotion;

    orchestrator and worker-Agent APIs using bounded child Work, isolated execution, budgets, and provenance;

    automated review gates that emit versioned Evaluation and Attention Artifacts;

    code-diff, test, security, and benchmark review in the human inbox;

    internal factory queues and promotion decisions.

5.16 Flexible commercial, billing, and unit-economics capabilities

The platform must support different commercial models by vertical and distribution path. It must not assume that the universal unit of value is a token, seat, or subscription.

Potential pricing and commercial models include:

    free or internal use;

    paid pilot or implementation fee;

    monthly or annual subscription;

    per-user, per-team, per-Instance, or per-environment pricing;

    per Work/job pricing;

    per delivered Artifact or service-level tier;

    usage credits for model, tool, compute, or external-provider consumption;

    retainer or managed service;

    developer hosting fee plus usage;

    expert-Agent subscription;

    publisher revenue share;

    enterprise committed-spend contract;

    bring-your-own-model/provider credentials with platform fees;

    outcome-linked or shared-savings pricing where attribution is credible;

    hybrid combinations of the above.

Commercial capabilities may require:

    accounts and organizations;

    Instances and developer projects;

    offers, plans, and entitlements;

    payer identity separate from user and acting Agent;

    usage facts attributed by Work, Execution, Artifact, Agent, Package, Instance, Source, channel, and external consumer;

    budget reservations and hard limits;

    credits, quotas, included allowances, and overage policy;

    trials and promotional access;

    subscriptions and contract terms;

    per-job quotes, acceptance, and delivery-based charging;

    publisher/developer revenue share;

    cost attribution to subscriber, publisher, developer, platform, model provider, or customer contract;

    gross-margin analysis by Work, Artifact, Agent, Package, Instance, Source, and channel;

    attention cost, support cost, and external-provider cost as well as token cost;

    suspension without data destruction;

    export and cancellation rights;

    private, unlisted, public, and developer-hosted offerings;

    tax, currency, invoice, payment, and settlement provider integration;

    abuse and rate controls.

Commercial-layer separation

The platform should keep these layers conceptually separate:

    Usage facts: measured consumption, cost, attention, and provider facts.

    Entitlement and budget: what is allowed and within which ceilings.

    Pricing policy: how an offer maps access, Work, delivery, usage, service level, or Outcome to money.

    Billing and collection: invoices, payment, tax, credits, and contract administration.

    Settlement and payout: allocation among platform, publisher, developer, customer, and provider.

The structural premise is accurate, durable usage and cost attribution. Those facts are not invoices.

Billing, pricing, payout, and payment-provider implementations should remain flexible commercial systems selected by the vertical and offer.

Outcome-based charging must be used only when Outcome definition and attribution are defensible; missing, noisy, or contested Outcomes must not be converted into fabricated billing certainty.
6. Capability matrix and preservation class

Legend for use-case relevance:

    Essential — intrinsic to the job.

    Common — likely in many products.

    Optional — selected products only.

Legend for preservation class:

    STRUCTURAL — Part 2 must preserve the identity, boundary, or historical relation.

    EXTENSIBLE — Part 2 needs an extension point, not necessarily a root object.

    PRODUCT — primarily a product or presentation decision.

Capability	Operate	Distribute	Improve	External / headless	Internal factory	Preservation
Purpose-built Experience	Essential	Common	Essential	Optional	Common	STRUCTURAL for Experience↔Operation bindings; PRODUCT for exact shell
Human-only Level 0	Essential	Common	Common	Optional	Common	STRUCTURAL for model-independent reads; PRODUCT for UI
Stable Actor and Agent identity	Essential	Essential	Essential	Essential	Essential	STRUCTURAL
Agent binding and scoped personalization	Common	Essential	Common	Common	Common	STRUCTURAL relation
Human/Agent/automation Operation parity	Essential	Essential	Essential	Essential	Essential	STRUCTURAL
Effect and reversibility classification	Essential	Essential	Essential	Essential	Essential	STRUCTURAL
Per-operation autonomy policy	Common	Common	Essential	Common	Essential	STRUCTURAL policy facts; EXTENSIBLE UX
Dry-run, compensation, concurrency	Common	Common	Essential	Common	Essential	STRUCTURAL Operation contract
Deterministic domain kernel	Optional	Optional	Common to Essential	Optional	Common	EXTENSIBLE driver; STRUCTURAL version reference when used
Heterogeneous Sources	Common	Common	Essential	Common	Essential	STRUCTURAL Source identity; EXTENSIBLE connectors
File projections and shell access	Common	Common	Common	Optional	Essential	EXTENSIBLE projection over STRUCTURAL grants
Domain Contract	Essential	Essential	Essential	Essential	Essential	STRUCTURAL
Analytical semantic engine / BSL	Common	Optional to Common	Common to Essential	Optional	Common	EXTENSIBLE engine; STRUCTURAL model/version provenance when used
Durable Work	Essential	Essential	Essential	Essential	Essential	STRUCTURAL
Bounded Agent job contract and delivery	Common	Essential	Common	Essential	Essential	STRUCTURAL Work↔Artifact↔delivery linkage; PRODUCT intake UX
Channel-first intake and cross-channel continuity	Common	Essential	Optional to Common	Essential	Common	STRUCTURAL Actor/Work linkage; EXTENSIBLE channel adapters
Stable execution/evidence join	Essential	Essential	Essential	Essential	Essential	STRUCTURAL
Recoverability and outcome-unknown	Essential	Essential	Essential	Essential	Essential	STRUCTURAL
Artifact identity and provenance	Essential	Essential	Essential	Essential	Essential	STRUCTURAL
Attention and approval plane	Essential for asynchronous Agent work	Essential	Essential	Common	Essential	STRUCTURAL linkage; EXTENSIBLE channels
Automation and triggers	Common	Common	Common	Common	Essential	EXTENSIBLE triggers; STRUCTURAL Actor/provenance
Untrusted-content handling	Essential	Essential	Essential	Essential	Essential	STRUCTURAL
Package trust and capability passport	Optional	Essential	Common	Common	Common	STRUCTURAL Package lineage/trust
Advice / Action / Attestation class	Common	Common	Essential in regulated products	Common	Common	STRUCTURAL metadata when used
Preference evidence	Common	Common	Essential	Common	Essential	STRUCTURAL linkage
Delayed real-world Outcomes	Common	Optional	Essential	Common	Essential	STRUCTURAL linkage; EXTENSIBLE capture
Evaluation and incumbent comparison	Optional to Common	Common	Essential	Optional	Essential	STRUCTURAL revision/incumbent relation; EXTENSIBLE evaluators
Controlled promotion and rollback	Common	Common	Essential	Optional	Essential	STRUCTURAL
Evaluation integrity and anti-gaming defenses	Optional	Common	Essential	Common	Essential	STRUCTURAL held-out/authorship separation; EXTENSIBLE detectors
Agent-generated declarative Experience	Common	Common	Common	Optional	Essential	EXTENSIBLE runtime; STRUCTURAL revision lineage
Package and Instance boundary	Common	Essential	Common	Common	Essential	STRUCTURAL
Upgrade safety and local overlays	Optional	Essential	Common	Optional	Common	STRUCTURAL lineage and migration receipts
Multi-Agent	Optional	Optional	Common	Optional	Common	STRUCTURAL cardinality/provenance; EXTENSIBLE routing
Multi-user collaboration	Common	Optional to Common	Common	Optional	Common	STRUCTURAL Actor on Work; EXTENSIBLE UX
Multi-application composition	Optional	Common	Optional	Optional	Common	STRUCTURAL namespacing; EXTENSIBLE shell
Local-to-cloud portability	Common	Common	Essential for sovereign domains	Common	Common	STRUCTURAL identity independence
Residency and export	Common	Common	Essential	Common	Common	STRUCTURAL policy metadata; EXTENSIBLE regions
Localization	Common	Common	Common	Common	Common	PRODUCT / EXTENSIBLE
Developer/operator tracing	Common	Common	Essential	Common	Essential	STRUCTURAL execution trace; EXTENSIBLE tooling
Developer framework, CLI, and hosted deployment	Optional	Common	Optional	Common	Essential	STRUCTURAL Package/release contract; EXTENSIBLE CLI and hosting
Orchestrator/worker code factory with review gates	Optional	Optional	Common	Optional	Essential	Reuses STRUCTURAL Work/Run/Artifact/Attention; EXTENSIBLE orchestration
Usage and attention facts	Common	Essential	Essential	Essential	Essential	STRUCTURAL attribution
Flexible pricing, billing, and settlement	Optional	Common	Optional to Common	Common	Common	EXTENSIBLE commercial policy over STRUCTURAL usage facts
Marketplace and automated payouts	Optional	Optional to Common	Optional	Optional to Common	Optional	EXTENSIBLE commercial layer

The matrix must not be read as a build list.
7. User-visible proof capabilities

The following features make the platform’s value understandable without exposing its architecture.
7.1 Named, costed, resumable Work

A user can leave “ACME renewal,” return on another device, see what happened, what remains blocked, how much it cost, and which outcomes later arrived.
7.2 Attention inbox instead of Agent babysitting

The user sees only what needs judgment, ranked by value, urgency, and risk. Every item opens exact context and can be answered from another approved channel.
7.3 Artifact with replay and explanation

A report, forecast, formulation, or draft shows:

    Sources and citations;

    Agent and Package revision;

    deterministic calculations;

    Operations and effects;

    human edits;

    cost and latency;

    ability to replay or compare.

7.4 Incumbent versus candidate everywhere

The user can compare two drafts, forecasts, formulations, Experiences, or Agent revisions; keep both; choose explicitly; and revert later.
7.5 Deterministic number with explanation

When a product presents a portfolio weight, nutritional result, forecast transformation, or constraint decision, the number comes from a versioned kernel and can be reproduced.
7.6 Unknown external effect that is visibly not retried

If an external provider response is ambiguous, the product shows “outcome unknown,” offers reconciliation, and never silently repeats the action.
7.7 Package capability passport and evaluation pack

A subscriber sees what a Package requests and what changed in a new version, including evaluation fixtures, known gains, regressions, authority, and expected cost.
7.8 Channel-first brief to delivered Artifact

A user or authorized external client can submit a bounded brief through Boring chat, WhatsApp, email, Slack/Teams, MCP, API, or another approved channel; receive questions and approvals without losing the Work; and obtain a durable Artifact with a delivery receipt. The same Work can move to a richer Boring Experience for review and later return to the original channel.
7.9 Local/degraded mode

When the model or remote host is unavailable, users can still inspect and export data, review prior Artifacts, resolve safe attention items, and run deterministic calculations.
7.10 Developer create-to-deploy path

A developer can scaffold an Agent or application with npx boring create, run and evaluate it locally, inspect its requested capabilities, deploy an immutable release with npx boring deploy, bind hosted Sources and secrets, observe Work and costs, and roll back without adopting a different hosted architecture.
7.11 Code factory review gate

An orchestrator delegates bounded child Work to worker Agents, workers produce code/test/release Artifacts, automated gates evaluate them, and only the items requiring judgment reach the human inbox with exact diffs, evidence, cost, and promotion choices.
7.12 The suspicious win that gets audited, not celebrated

When a candidate revision, formulation, or Agent shows an implausibly large improvement, the product visibly routes it to audit: the held-out evaluation, the gaming checks it passed, and the lineage of who authored the evaluator are shown before promotion is offered. The system treats “too good to be true” as a finding.
8. Representative capability bundles

These bundles are examples, not separate architectures.
8.1 Creator Studio

Operate domain work
+ domain navigation and Views
+ ambient Agent
+ content and document Artifacts
+ analytics Sources
+ optional semantic measures
+ publishing Operations
+ outcome capture
+ evidence-aware improvement

8.2 Creator’s Personal Agent

Distribute expert capability
+ expert knowledge and method
+ subscriber-private Sources
+ chat or small Experience
+ Artifact history
+ scoped personalization
+ Package / Instance boundary
+ subscription or entitlement later

8.3 Creator-Published SaaS

Operate
+ Distribute
+ brand and Experience
+ isolated subscriber Instances
+ subscriber-owned data
+ Package upgrades
+ local overlays
+ optional adaptive loop

8.4 Macro Research Terminal

Operate
+ public/private time-series Sources
+ Domain Contract
+ analytical semantic engine
+ deterministic transformations
+ charts and research Artifacts
+ data vintages
+ forecast Outcomes
+ incumbent comparison

8.5 Investment Workbench

Operate
+ private/public research Sources
+ company and portfolio semantics
+ deterministic risk and portfolio kernel
+ thesis Artifacts
+ Advice / Attestation distinction
+ approvals
+ portfolio Outcomes
+ optional specialist Agents
+ sovereign deployment

8.6 Industrial Formulation

Operate
+ Improve
+ proprietary Sources
+ ingredient/cost/constraint semantics
+ deterministic formulation kernel
+ candidate Artifacts
+ simulation and lab workflow
+ expert approval
+ delayed Outcomes
+ controlled method improvement

8.7 SME Pipeline Recovery

Operate
+ CRM/import Source
+ mail context
+ account and pipeline Views
+ stale-opportunity event triggers
+ follow-up drafts
+ human approval
+ reply and pipeline Outcomes
+ later Agent revision comparison

8.8 Boring Internal Code Factory

Operate internal product development
+ orchestrator Agent
+ bounded worker-Agent child Work
+ isolated branches/worktrees/sandboxes and narrowed Sources
+ code/declarative candidate Artifacts
+ tests, CI, security, and evaluation gates
+ review-gated Attention Items in the human inbox
+ cost and attention budgets
+ bounded acquisition experiments
+ human merge, release, deploy, and promotion decisions
+ package and Experience revision
+ production and commercial evidence returned to the next cycle

8.9 Channel-First Agent Job Service

bounded Job Contract
+ first-party chat / WhatsApp / email / Slack/Teams / voice / MCP / API intake
+ identity and entitlement resolution
+ secure attachment and Source connection
+ asynchronous Agent Work
+ questions and approvals through Attention
+ durable Artifact delivery
+ acceptance / revision / rejection
+ per-job, subscription, retainer, usage, or hybrid commercial model
+ optional later Outcome

8.10 Developer Platform and Hosted Agent Application

npx boring create
+ local Agent/app development
+ Operations, Sources, Views, and evaluation fixtures
+ npx boring dev / test
+ capability passport and deploy preview
+ npx boring deploy
+ hosted Instance, secrets, region, logs, budgets, and rollback
+ platform subscription or hosting/usage model
+ later Package publishing and distribution

9. Product maturity spectrum

Any product may move along this spectrum without changing its fundamental business identity.
Level 0 — Human-only

The application is complete domain software with the Agent disabled or unavailable.
Level 1 — Agent-assisted

The human initiates Work and the Agent answers or performs authorized Operations.
Level 2 — Personalized

The product uses explicit scoped preferences, memory, and local Experience overlays.
Level 3 — Evidence-aware

The product records Work, costs, Artifacts, accept/edit/reject signals, failures, and provenance.
Level 4 — Outcome-driven

The product links Work and Artifacts to delayed real-world outcomes, data vintages, and attribution confidence.
Level 5 — Evaluated and recursively improved

The product creates immutable candidate revisions, evaluates them against an incumbent, promotes through authorized decisions, monitors, and rolls back.

A mail client may remain at Levels 1–3.

A creator product may reach Level 4 once analytics are connected.

An industrial research product may target Level 5.
10. Business validation, selection criteria, and falsification
10.1 Shared success conditions
Value

    useful output;

    meaningful time saved;

    better decision or outcome;

    lower cost or increased throughput;

    access to expert capability;

    reduced human coordination burden.

Trust

    private data remains private;

    authority is bounded and revocable;

    untrusted content cannot create authority;

    consequential effects are approved or policy-controlled;

    results have provenance;

    failures and unknown effects are visible;

    revisions are reversible;

    customers can export their data.

Repeatability

    users complete repeated Work cycles;

    the value survives novelty;

    Work can be resumed and audited;

    the next customer or Instance requires less custom engineering.

Economics

    customers pay or an internal cost center explicitly funds the Work;

    the commercial model fits the vertical rather than forcing every offer into one universal billing unit;

    model, compute, external API, channel, and support costs are measurable;

    human attention is reduced rather than displaced into review;

    usage facts remain stable when pricing or payer policy changes;

    payer, user, publisher, developer, platform, and provider cost responsibilities are distinguishable;

    gross margin can improve;

    publisher/subscriber/developer economics remain understandable;

    one customer or developer can be onboarded with less custom engineering than the previous one.

Learning

    feedback joins to its originating Work and revision;

    preference and Outcome remain separate;

    comparison is statistically and causally honest enough for the decision;

    improvements do not rely on persuasive self-evaluation;

    customer evidence does not silently widen scope.

10.2 Primary repeatability measure

The most useful cross-product metric is second-cycle completion rate:

    The percentage of users or Instances that complete the core Work loop a second time on the same durable context without requiring a bespoke rescue by the builder.

The first cycle can be novelty.

The second cycle begins to indicate product value.
10.3 Minimum demonstrable loop by job
Operate

A user performs domain work in a purpose-built View. The Agent performs the same class of work through the shared domain Operation. The user reviews it through the attention plane, and later resumes the Work with full provenance.
Distribute

A publisher creates a Package. A subscriber receives an isolated Instance, connects one private Source, obtains a useful Artifact, and can accept a publisher upgrade without the publisher gaining access to subscriber data.
Improve

A candidate Artifact or revision is produced from evidence, evaluated against an incumbent under declared criteria, promoted by authorized decision, connected to a later Outcome, and rollback remains possible.
Channel-first / external / headless

A user or authorized external client begins with a brief through chat, WhatsApp, email, Slack/Teams, MCP, API, or another approved channel; a bounded Job Contract is admitted as durable Work; questions and approvals can be answered across channels; a durable Artifact is delivered; and later Outcome remains linkable without exposing the customer’s full private context.
Developer platform

A developer creates an Agent or application locally, runs the same Work and evaluation contracts used in hosting, deploys an immutable release through the CLI, binds hosted Sources and secrets, obtains a functioning Instance, observes traces and costs, and can roll back.
Internal code factory

An orchestrator admits and decomposes Work, bounded worker Agents produce code or Package Artifacts, automated review gates evaluate them, human-required decisions land in the inbox, and only authorized approval may merge, release, deploy, or promote the candidate.
10.4 Criteria for a strong domain

This section does not select the first domain. It defines a useful scoring rubric.
Criterion	Question
Outcome latency	How long between Agent work and a measurable result?
Outcome measurability	Can the result be captured automatically or reliably?
Decision frequency	Are there enough repeated cycles to learn?
Cost of error	Can mistakes be caught, compensated, or safely approved?
Data accessibility	Can the buyer connect useful Sources quickly?
Buyer proximity	Does the person with the pain control the purchase?
Value density	Is a modest improvement economically meaningful?
Attention savings	Does the product reduce human decisions or merely relocate them?
Deterministic kernel availability	Can critical calculations be grounded outside the LLM?
Sovereignty advantage	Is private hosting/governance a buying reason?
Regulatory load	Does regulation create differentiation or prohibitive friction?
Reference/distribution density	Is there an expert, creator, or network that already reaches the users?
Customization repeatability	Do customer variations look like overlays rather than bespoke rewrites?

A domain with slow or unobservable outcomes may still be a strong Level 1–3 product. It is a weaker choice for proving Level 4–5.
10.5 Conditions under which the thesis fails for a domain

Do not force a domain onto the platform when:

    meaningful work cannot be represented as domain Operations and remains only conversation;

    the Agent cannot perform meaningful work under any economically acceptable authority;

    outcomes cannot be observed even late or partially, yet the product is sold as self-improving;

    every token must be manually checked, so no attention is saved;

    deterministic calculations cannot be separated from model invention where correctness matters;

    untrusted content cannot be contained;

    the switching cost from generic ChatGPT plus documents is effectively zero because Boring holds no durable Work, Artifacts, authority, or operational integration;

    cost cannot be attributed to Work and unit economics cannot improve;

    subscriber and publisher data cannot be isolated;

    the product depends on a multi-app or marketplace ecosystem before one useful application exists;

    regulation makes the intended autonomous action legally or operationally impossible;

    evidence is too confounded to justify promotion, but the system promotes anyway.

These are product filters, not moral failures. Such a domain may still use ordinary chat or software. It should not shape the platform’s foundations.
11. Business questions intentionally left open

    Which job and domain become the first paid product?

    What is the first named Work noun a customer will see?

    Does the first buyer want a domain application, an expert Agent, or both?

    Which output class is being sold: Advice, Action, or Attestation?

    What meaningful Outcome can be observed within 30 days without pretending?

    What is the promised human attention budget?

    Which deterministic calculations must be outside the model?

    Which Sources are necessary, and which data must explicitly not become a Source?

    How expressive must declarative Views be before trusted code is required?

    How much Agent identity and memory should persist across Instances?

    Is subscriber-local personalization sufficient, or do buyers expect cross-product continuity?

    Which subscriber evidence may be exported to a publisher, organization, or platform?

    What evidence may improve a shared Package without revealing customer content?

    Can an expert method be captured primarily from the publisher’s own Work rather than manual authoring?

    Which external consumption door matters first: MCP, existing SaaS, email/chat channel, or API?

    How much application-building autonomy should an Agent receive before human review?

    Can Agent-generated declarative Experiences cover the first meaningful products?

    When does specialist multi-Agent work outperform one well-configured Agent?

    Which collaboration capability creates value before creating coordination overhead?

    Does one Work context ever need to span several independently entitled Packages?

    Is shared attention across applications sufficient before unified cross-app Work exists?

    Which domains require Swiss residency rather than general European residency?

    Which products create regulated advice or action, and who carries liability?

    How should erasure affect evidence that contributed to an already-promoted revision?

    How are publisher upgrades merged with subscriber overlays and data?

    What remains usable when the model or remote runtime is unavailable?

    Which outcome signals are robust enough for automated or policy-based promotion?

    Which evaluation signals must remain invisible to the Agents optimized against them, and who is allowed to refresh them?

    Is recursive improvement primarily customer value, an internal moat, or both?

    What is the smallest useful Package capability passport?

    Which usage facts are required before any billing model is selected?

    Which channel should provide the first low-friction job experience: first-party chat, WhatsApp, email, Slack/Teams, MCP, or another channel?

    What minimum Job Contract distinguishes a sellable delivered job from an open-ended conversation?

    Which verticals are best priced by subscription, seat/Instance, job, delivered Artifact, usage, retainer, hosting, or outcome—and who is the payer in each?

    Which parts of pricing, entitlement, billing, tax, collection, and settlement belong to Boring versus an external commercial provider?

    Should the developer path optimize first for Boring-hosted deployment, self-hosted framework adoption, or equal support for both?

    Which code-factory contracts from the current orchestrator/worker/review-inbox setup should become reusable platform capabilities, and which should remain internal process?

12. Capability handoff to Part 2

Part 2 should determine the smallest set of identities, boundaries, and relations required to satisfy this capability space.

It should not create one object per capability.
12.1 Candidate business noun budget

Part 2 should attempt to cover the space with roughly the following business identities, or explicitly justify a merge or split:

    Actor — human, Agent, automation, service, or foreign client.

    Authority — what the Actor may do now and under which constraints.

    Operation — typed shared domain behavior with effect and reversibility class.

    Source — governed data or service; not automatically a filesystem.

    Work — durable resumable unit independent of model, UI, and process.

    Execution identity — the stable join for one admitted unit of Work.

    Artifact — citable output with provenance and versions.

    Attention item — human judgment request linked to Work, Artifact, or Operation.

    Evidence — preference or Outcome linked to Work, Artifact, and revision.

    Package — immutable methodology and default Experience.

    Instance — isolated running application with owner-controlled data.

    Revision / incumbent relation — immutable candidate, comparison, promotion, and rollback.

Terms such as Page, View, Thread, Channel, external conversation, Job Contract, Delivery, native session, runtime environment, model, mount, trigger, developer project, billing plan, and evaluator are real, but Part 2 should not promote them to universal root objects merely because they are important capabilities.
12.2 Structural relations Part 2 must preserve

    every durable record belongs to a sovereign owner or Instance boundary;

    every consequential event names an Actor;

    Agent behavior is versioned and distinct from runtime process;

    Work is distinct from model-native session and process placement;

    one durable execution identity joins cost, effects, Artifacts, decisions, and delayed Outcomes;

    humans, Agents, and automations share domain Operations;

    authority is host-issued, narrowed, and revocable;

    Source content is data, not authority;

    Source, Knowledge, Memory, and Evidence remain distinct in scope and purpose;

    filesystems are one Source/projection kind, not the universal data model;

    Domain Contract is distinct from optional analytical semantics;

    deterministic kernels are versioned when used;

    preference evidence is distinct from real Outcome;

    revisions are immutable and activated through controlled promotion;

    Package is distinct from subscriber Instance and private data;

    Experience is a versioned projection over domain behavior, not the owner of it;

    deployment provider and shell implementation never define durable identity;

    package and application contributions are namespaced so cardinality can increase later;

    shared Work state is distinct from personal presentation state;

    a Channel or external conversation links to Work but never becomes its durable identity;

    produced output, delivered Artifact, and later successful Outcome remain distinguishable;

    evidence visible to an optimizing Actor is distinguishable from held-out evidence used to judge it, and the judged Actor cannot author or alter its own evaluator, fixtures, or held-out evidence;

    consequential execution facts originate from platform instrumentation and receipts, not solely from the acting Agent’s self-report;

    orchestrator, worker Agents, automated gates, and human reviewers use the same Work, Execution, Artifact, Attention, and promotion semantics;

    a developer deployment produces an immutable release/Package and hosted Instance rather than a special ad hoc runtime identity;

    usage facts are distinct from entitlement, pricing, billing, collection, and settlement;

    cross-boundary movement is explicit and auditable.

12.3 Extension points Part 2 should expose without multiplying the core

    Source and connector drivers;

    deterministic domain kernels;

    analytical semantic engines, including BSL;

    model and runtime providers;

    View renderers and workbench hosts;

    triggers, inbound/outbound channel adapters, and notification providers;

    orchestrator/worker strategies and automated review gates;

    developer SDK/CLI, template, build, deployment, and hosting providers;

    evaluator implementations;

    entitlement, pricing, billing, tax, payment, and settlement providers;

    package transports and registries;

    region and residency implementations;

    external Agent protocols;

    collaboration and composition UX.

12.4 Anti-proliferation rules for Part 2

    A capability does not automatically deserve an object.

    An extension adapter does not become a business identity.

    A UI concept does not own domain data or authority.

    A runtime placement does not own Work.

    An Agent definition does not grant itself authority.

    A semantic engine does not become the universal application model.

    An optimization record does not become mandatory for ordinary software.

    A marketplace concern does not redefine the Instance or Package boundary.

    Multi-Agent, multi-user, and multi-app are cardinality and relation problems before they are new platforms.

    A Channel adapter, developer CLI, billing plan, and code-factory orchestrator are clients or extensions of the same Work substrate, not parallel cores.

    A pricing model must not become an execution identity or evidence schema.

    The final architecture should be explainable through a small diagram and a named end-to-end Work flow.

12.5 Capability set Part 2 must account for

1. Purpose-built application Experience
2. Human-only degraded operation
3. Actor and Agent identity
4. Scoped memory and bindings
5. Human/Agent/automation/external-client Operation parity
6. Domain Contract and progressive Operation discovery
7. Effect, reversibility, autonomy, and approval semantics
8. Deterministic domain calculation
9. Governed heterogeneous Sources
10. Untrusted-content and cross-boundary controls
11. Optional analytical semantic querying and BSL
12. Durable Work and recoverability
13. Bounded Job Contract, delivery status, and Artifact handoff
14. Channel-first intake, cross-channel continuity, and secure delivery
15. Stable execution/evidence join
16. Artifacts, attention, and provenance
17. Preference and delayed Outcome evidence
18. Evaluation, incumbent comparison, and controlled promotion
19. Automation, triggers, and channel policies
20. Agent-generated declarative Experience changes
21. Package, Instance, trust, and upgrade boundaries
22. Developer framework, CLI, local/hosted parity, deployment, and rollback
23. Orchestrator/worker code factory with automated gates and human inbox review
24. Multi-Agent, multi-user, and multi-application extensibility
25. Local-to-cloud portability and sovereignty
26. Compliance, retention, export, and liability support
27. Developer/operator tracing and replay
28. Usage and cost facts separated from entitlement, pricing, billing, and settlement
29. Flexible vertical-specific commercial and payer models
30. Boring itself using the same Work and improvement substrate

The objective of Part 2 is to support this space with the fewest coherent identities—not to reproduce the capability list as an ontology.

