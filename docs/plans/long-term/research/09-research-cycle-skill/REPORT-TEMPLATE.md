<!-- Copy this file. Fill it in. Delete the comments. -->

# <Harvest | Ground | Challenge | Spike | Verify> — <target>

## Findings

<!-- One row per claim. No evidence, no row. Same columns everywhere so rows paste
     straight into the register and into issues.
     conf:   verified (call site traced) | executed (spike proved it) | reported (worker said so)
     status: open | fixed | disproven | wontfix
     `reported` must never trigger work. `disproven` rows are kept forever. -->

| id | statement | evidence | conf | sev | status |
|---|---|---|---|---|---|
| F-33-01 | <one falsifiable sentence> | `file.ts:120` or a quote | reported | high | open |

## Not run

<!-- Mandatory if anything was skipped. A silent gap reads as coverage. -->

- <what, and why — e.g. "eve dev — requires node>=24, host has 22">

## Contradicts

<!-- Prior findings this overturns. Overturning one is a success, not a failure. Omit if none. -->

- F-30-02 — <what was wrong and what replaces it>

## Detail

<!-- Prose, quotes, traces, pasted output. May explain a finding.
     May NOT introduce a claim that is absent from the table above. -->
