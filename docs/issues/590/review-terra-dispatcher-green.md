# Terra review — Slice 3A

**Verdict:** GREEN

**Model:** `openai-codex/gpt-5.6-terra`

- Trusted `userId` reaches namespace resolution in dynamic and pure dispatcher paths; full-app prefers explicit trusted identity.
- Dynamic normal-route and requestless-dispatcher cache keys align on root, namespace, and auth subject; integration coverage proves one harness/runtime.
- Malformed interrupt/stop receipts fail with stable `AGENT_CONTROL_RECEIPT_INVALID`.
- The bound dispatcher helper remains internal; only resolver types are exported from the server barrel.
- Core forwards the trusted-root and dispatcher composition seams.
- No blocker or high-severity finding remains.
