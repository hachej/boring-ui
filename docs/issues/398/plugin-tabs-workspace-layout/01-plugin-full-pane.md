# Deprecated — use 01-plugin-content-page.md

This file is kept only to avoid breaking links.

Use:

```txt
docs/plans/plugin-tabs-workspace-layout/01-plugin-content-page.md
```

Terminology correction:

```txt
Old: plugin full-pane mode
New: plugin content page inside current workspace
```

There is no special mode for phase 1. The plugin gets a workspace page/category slot and owns the React content inside it.
