# gh-1508 live Factory acceptance run (2026-09-03)

Model-driven run of the native Factory playground against a throwaway epic worktree. No merge, no Bead closed by agents.

| Item | Value |
| --- | --- |
| Epic key / branch | `live-farewell-r4` / `test/1508-live-epic-r4` (worktree base `6cf7c4939`) |
| Orchestrator | session `ec19243e-2a64-41da-b835-b7557c5e8db4`, `openai-codex:gpt-5.6-sol`, `/loop 120s` started and stopped |
| Bead | `factory-live-epic-1508-r4-qkqx`, label `epic:live-farewell-r4`, created by the Orchestrator |
| Worker | session `f12d4a05-d902-4628-bb18-49ecc9705680` (`openai-codex:gpt-5.4`), started via `dispatch_worker`; claimed with `br update … --claim --actor f12d4a05-…` |
| Commit | `e8f285352af03bc56f0dd38e6d6afd2f3ef89a1e`, exactly the three fixture files, pushed to `origin/test/1508-live-epic-r4` |
| Sandbox | lease `24b8f5ba-02c2-4d9a-aafc-e3bfd0f378ca`, HEAD verified equal to the commit, `npm test` 2/2, released |
| Fresh review | session `7ba9ad21-1d09-44fa-8f6b-06fc0f135956` (`boring-reviewer`, `openai-codex:gpt-5.4`), `VERDICT: approve`, `Reviewed-SHA: e8f2853…` |
| Handoff | full comment on the Bead (claim provenance, SHA, proof, release, review provenance, files, residuals, no-merge/no-close) |
| Orchestrator supervision | read back only Bead/git end-states; confirmed remote HEAD, exact-SHA proof, review provenance, no merge commits |

Earlier iterations and what they fixed:

1. Session create failed with `PATH_ESCAPE`: the loop extension lives in the app checkout, not the epic worktree. Fixed by authorizing the app checkout as a Pi resource root.
2. Local sandbox copied the whole worktree (inode exhaustion). Fixed by exact-SHA shared git clone snapshots.
3. Worker pulled repository-wide Beads. Fixed by host-injected epic binding (`epic:<key>` label) on every seat.
4. Worker claimed as the hostname and re-cloned from GitHub inside the sandbox. Fixed by stating the child session id in the delegated brief and documenting the sandbox as a committed-HEAD snapshot.
5. A 30s `/loop` kept the Orchestrator perpetually streaming; 120s is the working interval for a dispatch-and-verify loop.

Remote test branches left for inspection: `test/1508-live-epic` (run 3, commit `85d0540`) and `test/1508-live-epic-r4` (run 4). Delete them once reviewed.

## Crash-recovery run (2026-09-03, epic `live-farewell-r5`, branch `test/1508-live-epic-r5`)

Script: `apps/factory-playground/scripts/live-epic-recovery.mjs`. Orchestrator `9d7eace7…` planned Bead `factory-live-epic-1508-r5-aowb`, armed `supervise` at 90s, dispatched Worker `7bbb4170…`. The API was SIGKILLed with the claim held and three fixture files modified but uncommitted. After restart the supervision entry re-armed; the Orchestrator's tick read `factory_status`, released the stale claim with a comment, and dispatched recovery Worker `ab754bd1…`, which adopted the inherited edits, committed `8ac9529`, verified `.factory-sha` and `git rev-parse HEAD` in sandbox `73393da3…`, passed `npm test`, obtained `approve` from reviewer `e39ba648…`, pushed `origin/test/1508-live-epic-r5`, and recorded the handoff. Supervision stopped on request; nothing merged or closed.

## Crash-recovery run (2026-09-03, epic `live-farewell-r5`, branch `test/1508-live-epic-r5`)

Script: `apps/factory-playground/scripts/live-epic-recovery.mjs`. Orchestrator `9d7eace7…` planned Bead `factory-live-epic-1508-r5-aowb`, armed `supervise` at 90s, dispatched Worker `7bbb4170…`. The API was SIGKILLed with the claim held and three fixture files modified but uncommitted. After restart the supervision entry re-armed; the Orchestrator's tick read `factory_status`, released the stale claim with a comment, and dispatched recovery Worker `ab754bd1…`, which adopted the inherited edits, committed `8ac9529`, verified `.factory-sha` and `git rev-parse HEAD` in sandbox `73393da3…`, passed `npm test`, obtained `approve` from reviewer `e39ba648…`, pushed `origin/test/1508-live-epic-r5`, and recorded the handoff. Supervision stopped on request; nothing merged or closed.

## Remote-sandbox run (2026-09-03, epic `live-farewell-r6`, branch `test/1508-live-epic-r6`, provider `vercel`)

API launched with `BORING_FACTORY_SANDBOX_PROVIDER=vercel`, snapshot `snap_rr5UuvNuAQ8Zaa4iSv1eY3sS1VB0`, `BORING_FACTORY_REMOTE_SOURCE=fetch`. Orchestrator `ef7f25bb…` planned Bead `factory-live-epic-1508-r6-70n4`, armed `supervise` (120s), dispatched Worker `cd8cdb38…`. The Worker committed `e270a8a`, pushed, created Vercel lease `c89242fb…` which bootstrapped by fetching that exact SHA, ran `npm test` there (2/2), obtained `approve` from reviewer `a505ba5d…`, released the lease, and recorded the handoff. Supervision stopped on request via the tool; nothing merged or closed. Measured lease cost in fetch mode: about 8 seconds (create, seed, bootstrap, exec, dispose) versus about 14 minutes for the full-tree upload mode.

## Gated handoff run (2026-09-03, epic `live-farewell-r7`, branch `test/1508-live-epic-r7`, provider `vercel`)

Orchestrator `42d40c01…` planned Bead `factory-live-epic-1508-r7-zpxc`, then raised **Gate 1** in the Inbox (`[br-…zpxc] Plan approval: fixture farewell API`, overview with Bead graph, proof, risk, adversarial review outcome). On approve it armed `supervise` (120s) and dispatched Worker `b19dde05…`, which committed `50153e3`, pushed, verified the SHA in Vercel lease `b8673ea3…`, passed `npm test`, got `approve` from reviewer `bf9b1d68…`, and handed off. The Orchestrator then opened PR #1515 with the Owner Review card and a Handover section, started demo sandbox `5a4e59aa…` at the exact SHA (public URL, verified `Hello, Owner Goodbye, Owner.`), and raised **Gate 2** (`[br-live-farewell-r7] Merge approval: fixture farewell API`) with PR URL, SHA, demo URL and lifetime, please-test steps and handover lines. On approve it recorded the approval as a PR comment and did not merge; demo and supervision were stopped on request. The driver closed the test PR unmerged.

Lesson: the canonical `plan` block spent about 25 minutes on plan documents and two `codex exec` reviews before Gate 1; the precedence appendix now scales that ceremony to the epic.

## Convention run after skill reconciliation (2026-09-03, epic `farewell-api-r8`, branch `epic/farewell-api-r8`, provider `vercel`)

With the reconciled canonical skills and `BORING_FACTORY_FEATURE_NAME="Farewell API"`, the Orchestrator produced `[Farewell API] Epic` and `[Farewell API] Add farewell export, test, and docs` Beads, raised `[Farewell API] Plan approval` (first context line a plain sentence, ids after) after one bounded gpt-5.4 adversarial review, dispatched Worker `d7008bdc…` (commit `2f33f47`, exact-SHA Vercel lease, fresh review approve), opened PR #1516 `[Farewell API] Add farewell export, test, and docs` with the Owner Review card and Handover, started demo sandbox at the exact SHA, and raised `[Farewell API] Merge approval` with PR, SHA, demo URL and expiry. It disclosed an inherited CI blocker (stale root lockfile entry for the removed `pi-mono-loop`) instead of hiding it. Approval recorded as a PR comment; no merge; demo and supervision stopped; the driver closed the test PR unmerged. The lockfile is fixed in the same commit as this note.

## [Epic Closure] multi-Bead run (2026-09-03, epic `epic-closure`, branch `epic/epic-closure`)

First live run of `live-epic-acceptance.mjs`'s new multi-Bead mode (`EPIC_REQUEST_FILE`, `MIN_BEADS=3`, `MIN_WORKERS=2`, `EXPECT_DEPENDENCIES=1`), driving the Orchestrator through a real 3-slice feature request (`docs/issues/1508/epic-closure-request.md`) instead of the single-Bead fixture. Owner request: add epic closure (detect PR merge, close Beads, clean up branch/snapshot, raise an owner-facing Done acknowledgement) to the Factory playground itself.

**Epic worktree / branch:** `.worktrees/factory-epic-closure`, `epic/epic-closure`, pushed to `origin/epic/epic-closure`. **PR:** [#1518 "\[Epic Closure\] Finish Factory epics after merge"](https://github.com/hachej/boring-ui/pull/1518) — **OPEN, unmerged** at head `57f4c920607ee43a75a5ac6a283ed8d181b14800`.

**Beads and dependencies** (epic `factory-epic-closure-jgcj`, label `epic:epic-closure`):

| Bead | Title | Depends on | Final status |
| --- | --- | --- | --- |
| `factory-epic-closure-jgcj` | `[Epic Closure] Epic` | — | open (umbrella; closes last via `close_epic`, not this run) |
| `factory-epic-closure-jgcj.1` | `[Epic Closure] Detect merge and close Beads` | `.jgcj` (parent-child) | **closed** |
| `factory-epic-closure-jgcj.2` | `[Epic Closure] Clean up branch and snapshot` | `.jgcj.1` (blocks) + `.jgcj` | in_progress (re-proof under way at wrap-up) |
| `factory-epic-closure-jgcj.3` | `[Epic Closure] Add owner summary to the Inbox` | `.jgcj.1` (blocks) + `.jgcj` | in_progress (re-proof under way at wrap-up) |

Gate 1 (`[Epic Closure] Plan approval`, approved 20:07 UTC) correctly required `.2` and `.3` to depend on `.1`; the driver's dependency-count assertion (`br dep list`) observed 5 relations (3 parent-child + 2 blocks) and passed `MIN_BEADS=3`/`EXPECT_DEPENDENCIES=1`. After `.1` handed off the Orchestrator issued **two `dispatch_worker` calls in the same assistant turn** (confirmed via the tool-call trace) for `.2`/`.3` at 20:20:45 and again at 23:30:41 after the re-proof round — genuine 2-way parallel dispatch, satisfying `MIN_WORKERS=2`.

**Worker sessions** (all `[Epic Closure] Worker ← 55662374`, model `openai-codex:gpt-5.4`), in order: `8d431a17` (20:03) → `36921cce`/`d5538e6b` (20:20, parallel) → `1a688c6d` (20:33) → `53e501d6` (20:43) → `37a5b0a6` (21:29) → `26871ea8` (22:10) → `443227f6` (22:24) → `9e71284a` (22:27) → `a5c4e811` (22:28) → `e42621aa` (22:44) → `f607b08f` (22:48) → `bc10d376` (23:00) → `8f27873a` (23:13) → `12065ee6` (23:25, closed `.1`) → `4e192051`/`71f17406` (23:30, parallel, `.3`/`.2`). 19 `fresh_review` reviewer sessions ran across the epic, e.g. `ca602179`→`245c6683`, ending with `7666fbbf`/`643484b5`/`6f428c4a` all reviewing head `57f4c9206`.

**Everything that failed, in order, and what was changed:**

1. **Sandbox provider rejected as non-disposable** (`provider must implement the disposable sandbox profile`, `SANDBOX_LEASE_CREATION_ABORTED`). Root cause: `createPerEpicVercelProvider` in `apps/factory-playground/src/server/sandboxComposition.ts` returned a plain `SandboxProviderV1`, never attaching the `disposableProfile` `SandboxLeaseService` requires *before* the first `create()`. Fix: attach a static `disposableProfile` (contract `boring-sandbox.disposable-provider.v1`, `resume:false`, `publishedCleanupOwner:'returned-pair'`, `ambiguousCreate:'correlated-reconciliation'`, digest over `per-epic-vercel:<epicKey>:<remoteSource>`) synchronously at construction, independent of the lazy per-`create()` snapshot resolution. Exported `DisposableSandboxProviderProfileV1` from `packages/boring-sandbox/src/shared/index.ts` (wasn't public). Two new tests in `apps/factory-playground/src/server/sandboxComposition.test.ts` (profile present before any snapshot resolution + digest stability/uniqueness; accepted by a real `SandboxLeaseService`). API restart required (dist rebuild of `@hachej/boring-sandbox`); Worker/Orchestrator sessions and supervision survived per the crash-recovery model.
2. **History-rewrite incident on the shared epic branch, self-recovered.** A Worker `git commit --amend`ed pushed commit `f9c5131a1` into `1bf463fbe`; a later re-prove Worker (`37a5b0a6`) ran `git reset --soft HEAD~3` on the shared worktree, stranding 3 already-pushed commits as uncommitted state. Recovery: a plain `git fetch` + rebase onto `origin/epic/epic-closure` replayed the work with **no force-push** (verified: local HEAD matched `origin/epic/epic-closure` exactly throughout). Recorded as an incident comment on `.jgcj.1`. Root-cause fix landed upstream (owner commit `f8cec006b` on `feat/1508-factory-playground`): a "fix forward only, never rewrite pushed history" rule added to the Worker persona, the `exec` skill, and the host `factory-precedence` appendix; API restarted to load it.
3. **Weakened test assertion (honesty check).** Six commits (`660e70fe9` → `c1ccc159e`) progressively changed `factoryComposition.test.ts`'s `createSession` helper from asserting `201` to tolerating `AGENT_REQUEST_OUTCOME_UNKNOWN` (409) with an up-to-12-attempt **new-requestId** retry loop — masking a real signal instead of testing the actual idempotency contract (retry the *same* `requestId`, expect `200` + `existingSessionId`). Flagged via a Bead comment + Orchestrator prompt; a follow-up Worker reverted it forward in `75c1cb9c6` ("restore same-requestId session create contract") and a fresh review approved. `AGENT_REQUEST_OUTCOME_UNKNOWN` fires only when an effect is `in-flight` while the agent host is closing (`packages/agent/src/server/agent-host/createAgentHost.ts`); root cause not fully isolated, but it stopped recurring once test isolation was restored to the correct contract.
4. **Vercel sandbox creation blocked by account billing, HTTP 402.** Reproduced directly with `apps/factory-playground/scripts/vercel-lease-smoke.mts` against the same credentials the live API used: seed-sandbox creation failed immediately with `Status code 402 is not ok`. `GET /v2/teams/<id>` confirmed the team is on the `hobby` plan with no active billing period. First seen ~22:45 UTC. This is an **external account/billing constraint**, not a code defect — recorded as a Bead comment and reported rather than worked around. The API was switched to `BORING_FACTORY_SANDBOX_PROVIDER=local-simulation` for the remainder of the run (same `EPIC_WT`/`EPIC_KEY`/`FEATURE_NAME`; sessions/supervision survived the restart), and Gate 2 was run with `ALLOW_NO_DEMO=1` (new driver flag: skips only the demo-URL/liveness assertion when the Gate 2 context discloses "demo unavailable: provider quota"; never substitutes for per-Bead sandbox proof).
5. **Local-sandbox environment gap: gitignored plugin `dist/` missing.** Under `local-simulation`, `factoryComposition.test.ts` failed with `boring.front: resolved path escapes plugin root: dist/front/index.js` (e.g. `plugins/tasks/dist` absent from the local exact-SHA clone). A Worker iteratively patched around it in-repo (9 commits, `7401292c5`…`83f0e7626`) before the real host fix landed (owner commit `caaba4864` on `feat/1508-factory-playground`: local exact-SHA leases now link every gitignored `packages/*|plugins/*|apps/*` `dist`/`public` into the clone). Once the host fix was live, the Orchestrator reverted all 9 workaround commits forward (`cd2ee00db`…`57f4c9206`, all `git revert`s, preserving history) per instruction, and `.jgcj.1` was re-proven and **closed**.

**Sandbox/lease evidence (local provider, post-fix):** three dedicated local sandboxes independently verified `.factory-sha` and `git rev-parse HEAD` equal to the exact committed SHA; `pnpm exec tsc --noEmit` passed in all three. `pnpm exec vitest run` inside those same sandboxes still failed on the plugin-root-escape symptom for `.2`/`.3`'s re-proof at Gate 2 time (their rework was still in flight when the run was wrapped at the coordinator's 40-minute cutoff) — disclosed verbatim in Gate 2's context rather than hidden. **On the host directly** (not the sandbox), at epic head `57f4c9206`: `pnpm exec tsc --noEmit` — no errors; `pnpm exec vitest run` for the full `apps/factory-playground` suite — **7 files, 58 tests, all passed** (13–15s).

**Gate 1** — `[Epic Closure] Plan approval`, context first line: *"This plan makes a merged Factory epic finish itself safely and report completion to the owner."* Carried a `show-me-plan.md` artifact (structure/behavior/diff views + a mermaid sequence diagram) at `docs/issues/1517/show-me-plan.md`.

**Gate 2** — `[Epic Closure] Merge approval`, context first line: *"Epic Closure is presented for a merge decision with an unresolved sandbox test blocker."* Disclosed PR #1518, head SHA, "Demo unavailable: provider quota — Vercel returns HTTP 402 Payment Required," the exact sandbox `vitest` failure, and that final `fresh_review` verdicts were `request-changes` because full sandbox proof was still red — an honest presentation of an incomplete state rather than a manufactured pass. Approved by the driver (`decision: approve`, acknowledging the disclosed blocker); recorded as [a PR comment](https://github.com/hachej/boring-ui/pull/1518#issuecomment-5533642061), **no merge performed**; demo was never started (`PROVIDER_NOT_CONFIGURED` under `local-simulation`, consistent with `ALLOW_NO_DEMO=1`); supervision stopped and confirmed inactive.

**Driver changes** (`apps/factory-playground/scripts/live-epic-acceptance.mjs`): added the multi-Bead mode described above (env-gated, single-Bead mode unchanged); raised the idle-retry budget for `prompt()` from 40×3s to 200×5s (a busy Orchestrator under a 120s supervision tick can stay non-idle for minutes); added `RESUME_STAGE=gate2` to resume monitoring an already-approved-Gate-1 session without re-running planning; added `ALLOW_NO_DEMO=1` to skip only the demo-URL/liveness assertion at Gate 2 when the provider is out of quota.

**Net result:** epic closure's own core mechanism (`.1`: merge detection, `close_epic`, ordered Bead closure, demo/supervision teardown) is implemented, sandbox-proven, and closed; `.2` (branch/snapshot cleanup) and `.3` (owner Done acknowledgement) were mid-re-proof when the run was wrapped per the coordinator's honesty-over-completeness cutoff. PR #1518 stays open for owner review; nothing was merged; three real host bugs were found and fixed along the way (sandbox disposable-profile contract, local-sandbox gitignored-dist gap) and two agent-honesty incidents (history rewrite, weakened test assertion) were caught, disclosed, and corrected forward rather than hidden.
