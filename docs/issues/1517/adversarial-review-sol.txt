Verdict: REVISE
Material findings:
- HIGH — Remote-branch cleanup is simultaneously unreachable and under-guarded. The epic key normally equals the app checkout’s current branch ([sandboxComposition.ts](/home/ubuntu/projects/boring-ui-v2/.worktrees/factory-epic-closure/apps/factory-playground/src/server/sandboxComposition.ts:50)), so the current-branch prohibition makes normal cleanup always skip. Yet deletion lacks same-repository, head-ref, and expected-OID checks, risking deletion of a reused or advanced branch. Require `headRefName`, `headRefOid`, repository identity, validated refs, default/base-branch protection, and an exact-OID lease before remote deletion; treat an already-absent ref as idempotent success.
- HIGH — Partial-failure and retry semantics are not implementable safely. Both visuals close the epic before stopping runtime resources ([show-me-plan.md](/home/ubuntu/projects/boring-ui-v2/.worktrees/factory-epic-closure/docs/issues/1517/show-me-plan.md:30)), while the mitigation merely promises a receipt. Define a retryable phase contract: preflight PR/session/unique epic before mutation; query Beads with `--all`; treat already-completed operations as satisfied; retain failed demos; suppress epic closure and the Done acknowledgement until every required operation succeeds; stop calling-session supervision last. Specify whether requested cleanup failures prevent completion. Add failure-injection and rerun tests at every phase.
- MEDIUM — The named registry seams do not currently expose the required operations. Demo `close()` is a no-op and has no stop-all API ([demoPlugin.ts](/home/ubuntu/projects/boring-ui-v2/.worktrees/factory-epic-closure/apps/factory-playground/src/server/demoPlugin.ts:174)); supervision exposes only global timer closure, not durable per-session removal ([supervisionPlugin.ts](/home/ubuntu/projects/boring-ui-v2/.worktrees/factory-epic-closure/apps/factory-playground/src/server/supervisionPlugin.ts:51)); snapshot invalidation removes one `epicKey + lockfileSha256` entry although an epic can accumulate multiple keys ([snapshotRegistry.ts](/home/ubuntu/projects/boring-ui-v2/.worktrees/factory-epic-closure/apps/factory-playground/src/server/snapshotRegistry.ts:120)). Amend the Beads to require narrow host-only `stopAllDemos`, `stopSupervision(sessionId)`, and remove-all-entries-for-epic callbacks, with mixed-success and multiple-entry tests.
- MEDIUM — `factory_status.pr` has no stable unavailable contract: the plan alternates between omission and “unavailable” ([plan.md](/home/ubuntu/projects/boring-ui-v2/.worktrees/factory-epic-closure/docs/issues/1517/plan.md:24)). Define a discriminated JSON shape distinguishing `gh` unavailable, lookup failure, no PR, and available PR; define corresponding stable `close_epic` refusal codes and exact branch/PR-number matching.
- MEDIUM — Slice 3 lacks the data and exact schema needed for its acknowledgement. Slice 1’s receipt omits Worker-session IDs, while post-close status currently excludes closed Beads; additionally, `ask_user` radio fields require at least two options ([schema.ts](/home/ubuntu/projects/boring-ui-v2/.worktrees/factory-epic-closure/plugins/ask-user/src/shared/schema.ts:111)). Add involved Worker/calling-session IDs to the closure receipt and put a valid, explicit one-field radio schema with at least two options into the appendix contract. Require the acknowledgement only for a receipt whose overall state is `complete`.
Residual risks:
- The requested `br` commands could not run because this read-only environment prevented opening `.beads/.write.lock`. Direct SQLite/JSONL inspection shows exactly the epic plus slices `.1`, `.2`, `.3`; `.1` is ready and `.2`/`.3` are blocked only by `.1`.
- GitHub/Vercel availability and permissions remain external failure points; closure must continue to fail closed and preserve retryable state.