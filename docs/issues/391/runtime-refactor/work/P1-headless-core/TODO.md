# TODO-P1 — Headless core: `createAgent()`, dependency inversion, pure mode

Handoff: self-contained work order for one autonomous coding agent (pi or gpt-5.5-xhigh). Cite plan files by relative path. No access to prior conversation assumed.

## Context (read first)

Phase 1 of the #391 runtime refactor (v2) — the critical path. Goal: extract a Fastify-free `createAgent()` façade from the agent server, make `createAgentApp()` / `registerAgentRoutes()` thin adapters over it with **zero behavior change**, add a pure `runtime: 'none'` path, and prove the core has no ambient host-file authority. Phase 0 ([`../P0-adr/TODO.md`](../P0-adr/TODO.md)) must be merged first.

Required reading (relative to repo root):

- `docs/issues/391/runtime-refactor/architecture/00-global-isa.md` — package ownership, non-negotiable invariants 1–14, seams to reuse.
- `docs/issues/391/runtime-refactor/architecture/01-agent-core-runtime-free.md` — the pure-mode contract, the `AgentEnvironment` shape, and the **no-`AgentFeature`** rule (config uses the existing `tools`/`systemPromptAppend`/`systemPromptDynamic`/readiness seams directly; boring-bash contributes a plain `{ tools, readinessRequirements }` bundle the host spreads in), the pi-harness audit questions, required tests.
- `docs/issues/391/runtime-refactor/architecture/08-pluggable-agent-surfaces.md` — the `createAgent()` API surface (the **nine** members `start`/`stream`/`send`/`resolveInput`/`interrupt`/`stop`/`sessions`/`readiness`/`dispose`; `start`=accepted-receipt write, `stream`=replay+live-tail read, `send`=convenience over both, `interrupt(sessionId)`=abort current turn, `stop(sessionId)`=end/close session), the two-handles rule, the "façade has no Fastify import / no env reads / no file discovery" rule.
- `docs/issues/391/runtime-refactor/INDEX.md` — "Phase 1" deliverables + exit criteria (this TODO expands them).

Repo facts (verified against this worktree on 2026-07-04 — cite these exact paths/signatures, not older prose):

- **Two server shapes, both Fastify, both to become adapters:**
  - `packages/agent/src/server/createAgentApp.ts` — Shape A. `createAgentApp(opts: CreateAgentAppOptions = {}): Promise<FastifyInstance>` at lines 120–122. It value-imports `Fastify` at line 1, creates its own `Fastify({...})` instance at line 126, adds auth middleware via `createAuthMiddleware(...)` at lines 225–230, builds one static runtime bundle at lines 128–147, and constructs one `HarnessPiChatService` at lines 253–259.
  - `packages/agent/src/server/registerAgentRoutes.ts` — Shape B. `registerAgentRoutes: FastifyPluginAsync<RegisterAgentRoutesOptions>` at line 351. Host provides the Fastify app; this plugin wires request workspace scoping (`getWorkspaceId`/`getWorkspaceRoot` options at lines 309–310, request hook at lines 887–920), a `runtimeBindings` LRU (`runtimeBindings` line 371, `MAX_RUNTIME_BINDINGS = 256` line 372, eviction lines 373–379), and background provisioning (`startRuntimeProvisioning` begins line 530). This is the richer path.
  - Both are exported from `packages/agent/src/server/index.ts` (lines 98–99, 114–115).
- **The real turn loop is NOT `harness.sendMessage`.** The shared `AgentHarness` interface (`packages/agent/src/shared/harness.ts`, lines 24–78) has **no** `sendMessage`. The current input type is `SendMessageInput` at `harness.ts:89–98` (`sessionId: string`, `message: string`, optional `thinkingLevel`/`model`/`attachments`) and has type tests at `packages/agent/src/shared/__tests__/harness.test.ts:15–35` plus `harness.test-d.ts:13–21`. P1 renames/reconciles this to the single shared `AgentSendInput` type (see BBP1-002).
- **The streaming/control seam is `HarnessPiChatService`.** `packages/agent/src/server/pi-chat/harnessPiChatService.ts` defines `PiNativeHarness.getPiSessionAdapter?: (input: SendMessageInput, ctx: RunContext) => Promise<PiAgentSessionAdapter>` at lines 16–18 and `HarnessPiChatService implements PiChatSessionService` at line 61. The service creates/subscribes one `PiAgentSessionAdapter` channel at lines 448–523, maps native events through `PiChatEventMapper`, and publishes `PiChatEvent`s to `PiChatReplayBuffer`. `createPiCodingAgentHarness(...)` returns `getPiSessionAdapter(input, ctx)` at `packages/agent/src/server/harness/pi-coding-agent/createHarness.ts:663–670`, wrapping `createPiAgentSessionAdapter(...)`.
- **HTTP chat route** is `packages/agent/src/server/http/routes/piChat.ts` (registered as `piChatRoutes` at lines 125–305), served by `PiChatSessionService` (`piChat.ts:73–84`) and by `HarnessPiChatService`. There is **no** `http/routes/chat.ts` or `http/sse.ts`; the NDJSON event route is `GET /api/v1/agent/pi-chat/:sessionId/events` with `?cursor=` at `piChat.ts:178–231`, and replay/live-tail is `packages/agent/src/server/pi-chat/piChatReplayBuffer.ts`. Control routes already call `service.interrupt(...)` at `piChat.ts:276–284` and `service.stop(...)` at `piChat.ts:290–298`.
- **Current event type path:** `PiChatEvent` is not in `packages/agent/src/shared/chat.ts`; it is `packages/agent/src/shared/chat/piChatEvent.ts:6–43`, re-exported from `packages/agent/src/shared/chat/index.ts:17` and `packages/agent/src/shared/index.ts:62–87`.
- **Harness factory contract** (`packages/agent/src/shared/harness.ts`): `AgentHarnessFactory = (input: AgentHarnessFactoryInput) => AgentHarness | Promise<AgentHarness>` at line 22. Input includes `tools`, required `cwd`, `runtimeCwd?`, `systemPromptAppend?`, `sessionNamespace?`, `sessionRoot?`, `sessionDir?`, `systemPromptDynamic?`, `telemetry?` at lines 5–20. Default factory is `createPiCodingAgentHarness` (`packages/agent/src/server/harness/pi-coding-agent/createHarness.ts:333–359`), which currently requires `cwd: string` and returns a harness with `getPiSessionAdapter(...)`.
- **Runtime mode types** live in `packages/agent/src/server/runtime/mode.ts` (`RuntimeModeAdapter`, `RuntimeBundle`, `RuntimeModeId = 'direct' | 'local' | 'vercel-sandbox' | (string & {})`). Resolution is `packages/agent/src/server/runtime/resolveMode.ts` (`resolveMode()` / `autoDetectMode()`). `autoDetectMode()` reads `BORING_AGENT_MODE` and probes `bwrap`.
- **Session store** (`packages/agent/src/shared/session.ts`): `SessionStore { list, create, load, delete }` is at lines 1–6 and is keyed by `SessionCtx`; **current reality is `SessionCtx { workspaceId: string; userId?: string }` at lines 8–11**. **P1 change: `SessionCtx.workspaceId` becomes OPTIONAL — `SessionCtx { workspaceId?: string; userId?: string }`** (see BBP1-004). Pure/headless sessions (Slack-only deploys, embeds, plain Node) must **not fabricate a `workspaceId`**; when it is absent the store namespaces by the host-composed `sessionStorageRoot`. Pi impl: `packages/agent/src/server/harness/pi-coding-agent/sessions.ts` (`PiSessionStore`); session root default is `~/.pi/agent/sessions`, overridable via `BORING_AGENT_SESSION_ROOT` (`SESSION_ROOT_ENV` line 55). `PiSessionStoreOptions` already has `sessionRoot` and `sessionDir` at `sessions.ts:91–98`; P1 must surface `sessionStorageRoot` on `AgentConfig` and route it to existing `sessionRoot`, not invent a second storage concept. Current `PiSessionStore` ignores most ctx values for file lookup but still keys its list in-flight cache by `ctx.workspaceId` (`sessions.ts:122–130`), so optional ctx requires tests.
- **Existing invariant enforcement to extend:**
  - `packages/boring-bash/scripts/check-invariants.mjs` scans `packages/agent/src/**` at lines 59–70 and already fails on any **value** import or `require()` from `@hachej/boring-bash` (`import type` is allowed by the regex). Keep this script as the agent→boring-bash cycle gate; BBP1-006 adds proof that this existing gate still catches a negative fixture.
  - `packages/agent/src/__tests__/invariants.test.ts` runs root `scripts/check-invariants.sh` (ripgrep-based) against the package; BBP1-006 extends this source-level scan for direct `fastify` imports under the new `/core` façade source.
  - `packages/agent/scripts/check-agent-isolation.ts` (`check:isolation` script) walks `dist/` and currently forbids `@hachej/boring-core` specifiers. BBP1-006 extends this built-artifact scan to start at `dist/core/index.js`/`dist/core/index.mjs`, follow relative ESM imports through split chunks, and fail if any reachable file imports or requires `fastify`.
- **Package exports/build entries now:** `packages/agent/package.json` exports only `.`, `./shared`, `./server`, `./front`, `./front/styles.css`, and `./eval` at lines 12–35. There is no `./core` export yet. `packages/agent/tsup.config.ts` entries are `shared/index`, `server/index`, `front/index`, `eval/index` at lines 7–12, and `tsup.dev.config.ts` mirrors them at lines 7–12. BBP1-002 must add `core/index` to both configs and `./core` to package exports.
- **Existing public name collision:** `packages/agent/src/shared/config-schema.ts` already exports an unrelated `AgentConfig = z.infer<typeof ConfigSchema>` at lines 6–18 and `packages/agent/src/shared/index.ts` re-exports it at line 37. BBP1-002 must resolve this before exporting the façade `AgentConfig`: rename the old schema type to `AgentServerConfig` (or another explicit non-façade name) in the same PR, update `validateConfig(...)`, and update the shared barrel so `@hachej/boring-agent/core` owns the public façade `AgentConfig` name.
- **Consumers that must keep working unchanged through P1** (found via grep for `registerAgentRoutes`/`createAgentApp`) — **note: these in-repo composers break in TWO named cutovers, not one. P2 is the runtime-mode composition cutover (they inject the resolved runtime adapter instead of importing `resolveMode`, and the agent bin becomes pure-only — the first break lands here, at P2). P3 is the routes/tools composition cutover (they construct `createBashAgentFeature()` + mount `registerBashRoutes`). Each cutover is API-breaking for these composers, each migrated in its own PR; the "unchanged" promise here is a P1 promise, not a permanent one:**
  - `packages/cli/src/server/modeApps.ts` registers `agentServer.registerAgentRoutes` at line 671; the local-workspaces mode owns its YAML registry through `createLocalWorkspaceRegistry(opts.registryPath)` at line 452, whose default is `~/.boring-ui/workspaces.yaml` in `packages/cli/src/server/localWorkspaces.ts:27` — CLI-owned, already host composition.
  - `packages/core/src/app/server/createCoreWorkspaceAgentServer.ts` registers `registerAgentRoutes` at line 870.
  - `packages/workspace/src/app/server/createWorkspaceAgentServer.ts` calls `createAgentApp` at line 827.
  - `packages/agent/src/bin/boring-agent.ts` calls `createAgentApp` at line 125 and reads `BORING_AGENT_E2E_SCRIPTED_PI` at line 131; `packages/agent/src/server/dev.ts` calls `createAgentApp` at lines 10–15; `packages/agent/examples/with-custom-tool/server.ts` imports `createAgentApp` at line 9 and calls it at line 31.
- **E2E coverage that must stay green:** `packages/agent/e2e/` runs via `pnpm --filter @hachej/boring-agent run test:e2e` (`packages/agent/package.json:49`). Load-bearing specs present in this tree include `m2-modeflip.spec.ts`, `m3a-sessions.spec.ts`, `m3b-chat.spec.ts`, `m3c-interrupt-queue.spec.ts`, `bridge-protocol.spec.ts`, `pi-native-replay-gap.spec.ts`, `pi-native-multi-session-cold-reload.spec.ts`, `pi-native-long-transcript-reload.spec.ts`, `streaming-bash.spec.ts`, `pi-projection-ui.spec.ts`, and `bombadil/pi-native-chat.spec.ts`.

## Goal / exit criteria

Matches `INDEX.md` "Phase 1":

- `createAgent()` exported from its own **Fastify-free published subpath** `@hachej/boring-agent/core` (a new entry in `packages/agent/package.json` `exports`, following the existing `./shared`/`./server`/`./eval` subpath pattern), returning the **nine** members `{ start, stream, send, resolveInput, interrupt, stop, sessions, readiness, dispose }`. The subpath is the anchor the Fastify-graph invariant (BBP1-006) imports and proves clean. The write primitive is `start(input): Promise<{ sessionId, startIndex }>` (accepted receipt; the turn runs to completion on an independent producer regardless of any consumer); the read primitive is `stream(sessionId, { startIndex }): AsyncIterable<AgentEvent>` (same signature T1 will make durable). **P1 implementation is a minimal non-durable live tail over the current `HarnessPiChatService` / `PiChatReplayBuffer`; the durable `EventStreamStore` append/replay implementation lands in T1.** `send(input)` is documented convenience = `start` + `stream`; `interrupt(sessionId)` aborts the current turn and `stop(sessionId)` ends/closes the session (both wrap the existing `HarnessPiChatService` interrupt/stop — see BBP1-002). Producers are never consumer-backpressured; cancelling a stream iterator never cancels the turn; `interrupt()` is the only way to stop a running turn.
- `createAgentApp()` / `registerAgentRoutes()` are adapters over `createAgent()`; all current HTTP consumers behave identically (cli hub, workspace, core, agent-playground e2e). **This full compatibility promise holds only through P1** — there are **two named composition cutovers after it, and the first break is at P2, not P3**: **P2 is the runtime-mode composition cutover** (`TODO-P2` "This phase is the runtime-mode composition cutover") — `resolveMode` injection replaces the in-repo `resolveMode` imports and the agent bin becomes pure-only, so composers that resolved a mode already break there — and **P3 is the routes/tools composition cutover** (`TODO-P3` "This phase is the host-composition cutover") — each host must then construct `createBashAgentFeature()` + mount `registerBashRoutes` itself. The HTTP *wire* surface (route paths/behavior) stays byte-identical across both cutovers; only the composition API breaks, and each cutover migrates every consumer in its own PR.
- Typed config object only: **no** `process.env` reads, **no** `process.cwd()` defaults, **no** `.pi/*` discovery, **no** `workspaces.yaml` reads inside `createAgent()`. All such reads move to host/CLI composition and are passed in.
- A pure agent starts via `createAgent({ runtime: 'none' })` in a plain Node script with no Fastify, no workspace/sandbox/cwd/file routes/bash tools.
- `sessionStorageRoot` is separated from workspace roots.
- pi-coding-agent cwd/resource assumptions are audited; findings doc + follow-up seals produced (blocks pure-mode exit).
- Invariant tests: no agent value import from boring-bash; no Fastify in the façade module graph; smoke test `createAgent({ runtime: 'none' })` runs a turn with a fake harness in plain Node.

## Non-negotiables

- **Zero behavior change** for existing HTTP consumers. The adapters must produce byte-identical route surfaces and readiness semantics. Prove via the existing agent test + e2e suites, unchanged.
- `createAgent()`'s module graph must not transitively import `fastify`. This is invariant-tested (BBP1-006).
- No new plugin registry and **no `AgentFeature` abstraction** (single-consumer speculative abstraction — forbidden). `createAgent()` config exposes the existing seams directly — `tools`, `systemPromptAppend`/`systemPromptDynamic`, readiness (`mergeTools({ checkReadiness })`, `registerCapabilitiesContributor`). Boring-bash later contributes a plain `{ tools, readinessRequirements }` bundle (P3's `createBashAgentFeature()`) the host spreads into `tools` — see `01-agent-core-runtime-free.md` "Public core contracts".
- Type-only `RuntimeModeAdapter`/`RuntimeBundle` imports may stay in agent during migration; concrete mode adapters (`direct`/`local`/`vercel-sandbox`) and `resolveMode()` stay put in P1 (they move to boring-bash in Phase 2). P1 only inverts *who calls* them: host composition, not `createAgent()` internals.
- Pure mode must pass **no cwd** or a sealed virtual root with no host files to the harness — stronger than "omit cwd from prompt" (invariant 1, `00-global-isa.md`).
- Session-history durability is separate from file/workspace durability (invariant 5).
- `SessionCtx.workspaceId` is **OPTIONAL**. Pure/headless sessions (Slack-only deploys, embeds, plain Node) omit it; the session store namespaces by the host-composed `sessionStorageRoot` when it is absent. **Surfaces MUST NOT synthesize a fake `workspaceId`** to satisfy the store. **Cross-reference (`09-environments-attachable.md` security invariant 5):** a session may omit `workspaceId`, but **environment attachments (`company_context`, any governed fs, MCP projection) REQUIRE a workspace-bound context** — a workspace-less session runs `runtime: 'none'` with **no attachments** until the host binds it to a workspace; it never synthesizes a `workspaceId` to attach one.

## Do NOT

- Do NOT move provider implementations to `boring-bash` in this phase — that is Phase 2 and requires the injection seam from here first (`01` "Dependency inversion first").
- Do NOT invent a new streaming/event protocol — reuse `HarnessPiChatService`. The indexed `AgentEvent` envelope + durable offset-addressed event log are Phase T1; in P1 `stream` is a **minimal non-durable live tail** over the existing `HarnessPiChatService`/replay-buffer path (so `send` = `start` + `stream` works end-to-end), and only *historical* replay from an offset older than the live buffer throws `AgentNotImplementedError` / `ERR_NOT_IMPLEMENTED_UNTIL_T1`. `resolveInput` remains a typed stub throwing the same error (see BBP1-002).
- Do NOT add env-var reads or file discovery inside `createAgent()`. If the façade needs a value that today comes from `process.env`, take it as a config field and let the adapter/host read the env.
- Do NOT change `workspaces.yaml` handling in the CLI — it is already host composition (`packages/cli/src/server/modeApps.ts`); just confirm the façade does not re-read it.
- Do NOT re-export moved values from agent barrels in a way that creates a cycle.

## Beads

### BBP1-001 — Config-surface inventory: enumerate every env/cwd/file-discovery read in agent server code — M

- **Description:** Produce the exhaustive list of ambient reads inside `packages/agent/src/server/**` that must move to host/CLI composition, so BBP1-002/003 can turn each into a typed config field. This is a research + documentation bead feeding the façade design.
- **Files to touch/create:** create `docs/issues/391/runtime-refactor/work/P1-headless-core/_p1-config-surface.md` (the inventory table). No product code.
- **PR-PLAN mapping:** `PR-PLAN.md` P1 row `pr1-config-inventory` (`BBP1-001`, doc, gate: grep reproducers resolve).
- **Implementation notes — seed inventory verified from current source; complete with the reproducers below:**
  - `process.cwd()` defaults: `createAgentApp.ts:123` (`opts.workspaceRoot ?? process.cwd()`), `registerAgentRoutes.ts:352`, `config/workspaceRoot.ts:3`, `models/modelConfig.ts:236` (`SettingsManager.create(process.cwd(), getAgentDir())`), `sandbox/direct/createDirectSandbox.ts:74`. Also record CLI/bin/test-only defaults separately: `packages/agent/src/bin/boring-agent.ts:31`, `packages/agent/src/server/__tests__/**`, etc. The target inventory is `packages/agent/src/server/**`; non-server callers are evidence of host-owned composition, not façade inputs.
  - Env vars via `getEnv()`/`process.env`:
    - `BORING_AGENT_TEMPLATE_PATH` — `createAgentApp.ts:125`, `registerAgentRoutes.ts:354`.
    - `BORING_AGENT_MODE` — `runtime/resolveMode.ts:26` (`autoDetectMode`).
    - `BORING_AGENT_WORKSPACE_ROOT` — `config/workspaceRoot.ts:4` (`WORKSPACE_ROOT_ENV`).
    - `BORING_AGENT_SESSION_ROOT` — `harness/pi-coding-agent/sessions.ts:55` (`SESSION_ROOT_ENV`); default `~/.pi/agent/sessions`.
    - `BORING_MAX_WATCHED_ENTRIES` — `workspace/nodeWatcher.ts:77`.
    - `BORING_WORKER_BASE_URL`, `BORING_WORKER_INTERNAL_TOKEN` — `runtime/modes/remote-worker.ts:28–29`.
    - `BORING_AGENT_UV_BIN` — `workspace/provisioning/provisionWorkspaceRuntime.ts:217`.
    - Model-config family (`models/modelConfig.ts:35–255`): `BORING_AGENT_INFOMANIAK_*` including `BORING_AGENT_INFOMANIAK_MODELS` and compatibility knobs (`_SUPPORTS_STORE`, `_SUPPORTS_DEVELOPER_ROLE`, `_SUPPORTS_REASONING_EFFORT`, `_SUPPORTS_USAGE_IN_STREAMING`, `_MAX_TOKENS_FIELD`, `_REASONING`, `_INPUT`, `_CONTEXT_WINDOW`, `_MAX_TOKENS`); `BORING_AGENT_CUSTOM_MODEL_*` with the same compatibility suffix family; `BORING_AGENT_DEFAULT_MODEL` / `_ID` / `_PROVIDER`.
    - `BORING_AGENT_VERBOSE` — `server/logging.ts:64`.
    - `getEnvSnapshot()` / `process.env` spread: `sandbox/workspacePythonEnv.ts:30`, `runtime/modes/provisioningAdapter.ts:86`, `workspace/provisionRuntime.ts:71`.
    - Config parsing/schema surface: `config/env.ts:1–17` (`getEnv`, `getEnvSnapshot`, `setEnvDefault`), `config/loadEnv.ts:14–22` defaults to `process.env`, and `shared/config-schema.ts:20–55` names the currently accepted config env keys, including `BORING_AGENT_MODEL`, `BORING_AGENT_CONFIG`, `BORING_AGENT_DEV`, `BORING_AGENT_NO_OPEN`, `BORING_AGENT_NO_GITIGNORE`, `BORING_AGENT_SNAPSHOT_KEEP`, `BORING_AGENT_PYTHON_PACKAGES`, Vercel token/team/project keys, and `BORING_AGENT_VERCEL_SANDBOX_TIMEOUT_MS`.
    - Vercel sandbox env defaults/reads: `runtime/modes/vercel-sandbox.ts` defines `BORING_AGENT_VERCEL_SANDBOX_TIMEOUT_MS` / `BORING_AGENT_VERCEL_SANDBOX_RUNTIME` at lines 46–47 and seeds `BORING_AGENT_UV_BIN` via `setEnvDefault(...)` at line 276. Treat as provider/host composition, never façade logic.
    - Test-only: `BORING_AGENT_E2E_SCRIPTED_PI*` (`bin/boring-agent.ts:131`, `testing/scriptedPiHarness.ts`).
  - File discovery / home-dir reads:
    - `.pi/` plugin discovery: `harness/pi-coding-agent/pluginLoader.ts:10–12` — `GLOBAL_DIR = ~/.pi/agent/extensions`, `LOCAL_DIR = .pi/extensions`, `EXTENSIONS_JSON = .pi/extensions.json`. The loader also scans `node_modules/pi-plugin-*` (`pluginLoader.ts:97–108`) and `.pi/extensions.json` npm/git entries (`pluginLoader.ts:142–200`). Triggered by `loadPlugins({ cwd })` in both server shapes when `externalPlugins !== false` **and** `modeAdapter.workspaceFsCapability === 'strong'` (`createAgentApp.ts:155–157`, `registerAgentRoutes.ts:617–618`).
    - Pi auth/settings/resources: `AuthStorage.create()` / `getAgentDir()` / `SettingsManager.create()` / `DefaultResourceLoader(...)` — `harness/pi-coding-agent/createHarness.ts:444`, `:490`, `createResourceSettingsManager(...)` at `:215–247`, call site `:497–501`, resource loader `:502–530`; `http/routes/models.ts:48`; `models/modelConfig.ts:236`.
    - `workspaces.yaml` — consumed only in `packages/cli/**`: default `~/.boring-ui/workspaces.yaml` in `packages/cli/src/server/localWorkspaces.ts:27`, and workspaces mode builds the registry in `packages/cli/src/server/modeApps.ts:452`. Record as "already host-owned; confirm façade does not re-read".
  - For each row record: file:line, what is read, whether it feeds `createAgent()` config or stays inside the (host-composed) harness/provider, and the target config field name.
  - Reproducer commands to paste into `_p1-config-surface.md` after running:
    - `rg -n "process\\.cwd\\(|getEnv\\(|process\\.env|BORING_|getEnvSnapshot\\(|setEnvDefault\\(" packages/agent/src/server packages/agent/src/shared/config-schema.ts packages/agent/src/bin`
    - `rg -n "loadPlugins\\(|GLOBAL_DIR|LOCAL_DIR|EXTENSIONS_JSON|AuthStorage\\.create|SettingsManager\\.create|getAgentDir\\(|DefaultResourceLoader|SessionManager\\.create|noContextFiles|noSkills|workspaces\\.yaml" packages/agent/src packages/cli/src`
- **Tests to add:** none (doc). The grep commands used become the reproducer in the doc.
- **Acceptance:** `_p1-config-surface.md` exists and contains (1) the two reproducer commands above with their current outputs summarized, (2) every ambient read in `packages/agent/src/server/**` classified as (a) typed config field on `createAgent()`, (b) stays in host/provider/harness composition and is not reachable from `createAgent()`, or (c) test-only/ignore, and (3) zero `UNKNOWN` / `TBD` rows.

### BBP1-002 — `createAgent()` façade (Fastify-free) — L

- **Description:** Add `createAgent(config): Agent` published from the canonical Fastify-free entry `@hachej/boring-agent/core` (impl file under `src/server/createAgent.ts`, re-exported by the `./core` entry; the `@hachej/boring-agent/server` barrel may re-export it for convenience but carries no contract status), returning the **nine** members `{ start, stream, send, resolveInput, interrupt, stop, sessions, readiness, dispose }`. Fastify-free. Config shape derived from what `createAgentApp` currently reads (BBP1-001), passed in — never read from env inside the façade. `start` is the write primitive (accepted receipt), `stream` is the offset-shaped read primitive (P1 live-tail over the existing replay buffer; durable historical replay lands in T1), `send` a convenience over both, `interrupt(sessionId)`/`stop(sessionId)` the turn-abort / session-end control pair — see `08` "Producer/consumer split".
- **PR-PLAN mapping:** `PR-PLAN.md` P1 row `pr2-createagent-facade` (`BBP1-002`, split allowed as `pr2a` types/export/facade skeleton and `pr2b` producer-consumer wiring if the cap is hit).
- **Files to touch/create:**
  - create `packages/agent/src/server/createAgent.ts` (the façade + `AgentConfig` type).
  - create `packages/agent/src/core/index.ts` (new): the **Fastify-free public entry** that re-exports `createAgent` + `AgentConfig` (+ the public `Agent`/`AgentEvent` types). Add a `./core` subpath to `packages/agent/package.json` `exports` (and a tsup/build entry), following the existing `./shared`/`./server`/`./eval` subpath pattern. This dedicated entry — whose closure imports no adapter — is what makes the Fastify-free guarantee mechanically checkable (BBP1-006 imports this exact published path). `@hachej/boring-agent/server` may still re-export `createAgent` for convenience, but the Fastify-free contract is anchored on `@hachej/boring-agent/core`.
  - create `packages/agent/src/shared/events.ts` (new): the **single public `AgentEvent` envelope type** (`{ v: 1, eventIndex: number, timestamp: number, sessionId: string, chunk: PiChatEvent }`) plus `Agent`, `AgentConfig`, `AgentSendInput`, and `AgentNotImplementedError` / `ERR_NOT_IMPLEMENTED_UNTIL_T1` public types/classes on the shared (type-only/front-safe except the error class: no `node:*`, no `Buffer`, no Fastify). Import `PiChatEvent` from `./chat` (actual type is `shared/chat/piChatEvent.ts:6–43`, re-exported by `shared/chat/index.ts:17`). **P1 creates this file** — `createAgent()`'s `stream()` signature returns `AsyncIterable<AgentEvent>` and needs the type to compile. **T1 does not create `events.ts`; it extends/uses this P1 file** (durably persisting the envelope, adding `ResolveInputResponse`, filling in `sessionStreamPath`). Do not leave `AgentEvent` ownership hedged to T1.
  - update `packages/agent/src/shared/config-schema.ts` / `src/shared/index.ts` to resolve the existing non-façade `AgentConfig` name collision (`shared/config-schema.ts:18`, shared barrel line 37). Pin: rename the old schema type to `AgentServerConfig` in the same PR; the public `AgentConfig` name belongs to the façade config exported by `@hachej/boring-agent/core`.
- **Implementation notes:**
  - Derive `AgentConfig` from `CreateAgentAppOptions` minus HTTP/Fastify concerns. Concretely it carries: `harnessFactory?` (default `createPiCodingAgentHarness`), `runtime: RuntimeModeAdapter | 'none'`, `tools?: AgentTool[]` (extra app-owned tools — the boring-bash bundle's tools are spread in here by the host, there is **no** `features?` member), `readinessRequirements?: string[]` (opaque gates, host-supplied), `sessions?` override, `systemPromptAppend?`, `systemPromptDynamic?`, `telemetry?`, `metering?`, `sessionStorageRoot?` (see BBP1-004), and the resolved non-env values that were previously env-read (template path, workspace/session roots) — all supplied by the caller. Do **not** add an `AgentFeature`/`features` config surface.
  - **Single send-input type — `AgentSendInput` (shared).** Name the `send` input `AgentSendInput`, reconciled from the existing `packages/agent/src/shared/harness.ts` `SendMessageInput` (extend and rename — **one** type, defined in `shared/events.ts`; do not keep two parallel input types). `SendMessageInput`'s callers and tests migrate to `AgentSendInput` in the same PR: `shared/harness.ts`, `shared/__tests__/harness.test.ts`, `shared/__tests__/harness.test-d.ts`, `server/pi-chat/harnessPiChatService.ts`, `server/harness/pi-coding-agent/createHarness.ts`, `server/testing/scriptedPiHarness.ts`, and pi-chat tests currently importing `SendMessageInput`. `AgentSendInput` carries the normalized user turn (`{ sessionId?, content, attachments?, actor, ctx?: SessionCtx, originSurface?: string }` — omit `sessionId` to create a new session) and is the type T2's `ChatTransport.sendMessages` and 08's message-in shape both reference. **P1 defines the FULL type here (in shared), including `ctx?` and `originSurface?`:** `ctx?: SessionCtx` is boring's tenancy context resolved by the adapter/host (the `SessionStore` scoping key — see BBP1-004's optional-`workspaceId` rule; **never** surface-native platform addressing); `originSurface?: string` is session-create provenance written by adapters (workspace/Slack/embed) and consumed by S3's origin badge. T2's originSurface bead (`TODO-T2` BBT2-001) **EXTENDS** this type — it specifies the session-create provenance semantics and populates the field, it does **not** redefine the type. All façade calls are single-argument (`start(input)`, `send(input)`; never `send(input, ctx)`).
  - `agent.start(input: AgentSendInput): Promise<{ sessionId: string; startIndex: number }>` — the write primitive. If `input.sessionId` is absent, create a session through `agent.sessions.create(input.ctx ?? {})` and use the returned id; if present, use it unchanged. Admit the turn onto `HarnessPiChatService` (`packages/agent/src/server/pi-chat/harnessPiChatService.ts`): build the service once per agent from `{ harness, sessionStore: harness.sessions, workdir, workspace?, metering }`, subscribe/live-tail before calling `prompt`, drive `service.prompt(ctx, sessionId, payload)` on an **independent producer** that runs to completion regardless of any consumer, and return `{ sessionId, startIndex }` as soon as the prompt is accepted (use the accepted `PromptReceipt.cursor` as `startIndex`). Producers are never consumer-backpressured. In P1 the producer's events are wrapped from the existing `PiChatEvent` stream into `AgentEvent`; durable append is T1. Keep the async-iterable/subscriber shape so T1 can wrap it without an API break.
  - `agent.stream(sessionId, { startIndex }): AsyncIterable<AgentEvent>` — the read primitive (same signature T1 will make replay-from-offset + live-tail; no separate `replay()` method). **Minimal non-durable implementation in P1** — use the existing `HarnessPiChatService.subscribe(ctx, sessionId, cursor, subscriber)` / `PiChatReplayBuffer.subscribe(...)` path (`harnessPiChatService.ts:163–168`, `piChatReplayBuffer.ts:94–137`) so `send()` = `start` + `stream` yields events end-to-end. Wrap each `PiChatEvent` as `{ v: 1, eventIndex: event.seq, timestamp: Date.now(), sessionId, chunk: event }`. Historical replay older than the live buffer (`PI_CHAT_REPLAY_GAP` or cursor out of range) throws `AgentNotImplementedError` with code `ERR_NOT_IMPLEMENTED_UNTIL_T1`; live-tail streaming works in P1. Cancelling the returned iterator calls the subscription's `unsubscribe()` only and must never abort the running prompt.
  - `agent.send(input: AgentSendInput): AsyncIterable<AgentEvent>` — documented convenience only: `const { sessionId, startIndex } = await start(input); yield* stream(sessionId, { startIndex })`. Because P1's `stream` provides a working live tail, `send` is functional end-to-end in P1 (it drives one turn and yields its events); keep it defined purely as the composition with no independent semantics, so T1's durable swap flows through unchanged.
  - `agent.sessions` = `config.sessions ?? harness.sessions` (a `SessionStore`). If `config.sessions` is provided, thread it to `HarnessPiChatService` as `sessionStore` so HTTP and façade share the same store.
  - `agent.readiness` = the per-requirement view already produced by `createRuntimeReadyStatusTracker(...)` (`runtime/modeReadiness.ts`) + `mergeTools({ checkReadiness })`. Surface it as data, not routes.
  - `agent.resolveInput(...)` — **stub in P1** (real approvals path is T1). Provide the method signature `(sessionId, requestId, response)` and throw the typed `AgentNotImplementedError` (`ERR_NOT_IMPLEMENTED_UNTIL_T1`). Document the T1 hand-off.
  - **Typed stub error:** define `AgentNotImplementedError` (code `ERR_NOT_IMPLEMENTED_UNTIL_T1`) in the shared/type-only surface; `resolveInput` throws it unconditionally, and `stream` throws it **only** for a historical `startIndex` older than the live buffer (live-tail streaming works in P1). **Do NOT** ship any downstream pack (T2, S1, P3, …) that depends on durable historical replay or on approvals — they block on T1 filling these in.
  - `agent.interrupt(sessionId)` / `agent.stop(sessionId)` — **real in P1** (not stubs): thin wrappers over the existing `HarnessPiChatService.interrupt(ctx, sessionId, {})` and `HarnessPiChatService.stop(ctx, sessionId, {})` methods (`harnessPiChatService.ts:273–299`), the same control seam `piChatRoutes` invokes today (`piChat.ts:276–298`). `interrupt(sessionId)` aborts the current turn (the running producer) without closing the session; `stop(sessionId)` ends/closes the session and clears queued follow-ups. Do not invent a new abort path.
  - `agent.dispose()` — dispose the mode adapter (`modeAdapter.dispose?.()` when `runtime !== 'none'`) and any owned harness/service resources. If the current harness has no dispose method, document this as a no-op and do not add speculative lifecycle APIs to `AgentHarness`.
  - When `runtime: 'none'`: build no `RuntimeBundle`, register no file/bash tools/features, and construct the harness with a sealed/absent cwd (see BBP1-004 + BBP1-005).
- **Tests to add:** unit test that `createAgent({ runtime: 'none', harnessFactory: fakeHarness })` constructs without Fastify and exposes exactly the **nine** members `start`, `stream`, `send`, `resolveInput`, `interrupt`, `stop`, `sessions`, `readiness`, `dispose`; `send` yields at least one event from a fake harness turn via the live tail (fuller smoke is BBP1-006); a `stream(sessionId,{startIndex})` call with an offset older than the live buffer throws `AgentNotImplementedError`/`ERR_NOT_IMPLEMENTED_UNTIL_T1`; `interrupt(sessionId)` on an in-flight fake turn aborts it (the producer stops appending) without closing the session, and `stop(sessionId)` ends the session — both delegating to the fake `HarnessPiChatService` interrupt/stop.
- **Acceptance:** `packages/agent/src/server/__tests__/createAgent.pure.test.ts` proves `createAgent()` imports from `@hachej/boring-agent/core` with no Fastify, exposes exactly the nine keys, `send` yields at least one `AgentEvent` from a fake harness, historical `stream` throws `AgentNotImplementedError`/`ERR_NOT_IMPLEMENTED_UNTIL_T1`, and `interrupt`/`stop` call the fake service control methods. `pnpm --filter @hachej/boring-agent run test -- createAgent.pure.test.ts` passes.

### BBP1-003 — Make `createAgentApp()` + `registerAgentRoutes()` thin adapters — L

- **Description:** Refactor both server shapes so they *compose* `createAgent()` (or share its internals) and only add HTTP concerns (Fastify instance, auth middleware, route registration, request→workspace scoping, LRU bindings). Zero behavior change.
- **PR-PLAN mapping:** `PR-PLAN.md` P1 row `pr3-adapters-thin` (`BBP1-003`, refactor, gates: full agent `test` + `test:e2e`).
- **Files to touch:** `packages/agent/src/server/createAgentApp.ts`, `packages/agent/src/server/registerAgentRoutes.ts`. Do not change the exported option types' observable behavior.
- **Implementation notes:**
  - Keep `CreateAgentAppOptions` / `RegisterAgentRoutesOptions` as the public HTTP-adapter option types. P1 compatibility pin: the adapters may still resolve their current ambient inputs (`process.cwd()`, `getEnv('BORING_AGENT_TEMPLATE_PATH')`, `autoDetectMode()` / `resolveMode(...)`, `loadPlugins({ cwd })`) exactly where they do today, then build an `AgentConfig` and call `createAgent({ runtime: modeAdapter, ... })`. The façade never performs these reads. P2 is the cutover that removes built-in `resolveMode()` value imports from agent composition.
  - `registerAgentRoutes` is the hard one: its per-request `RuntimeBinding` LRU (`createRuntimeBinding`, `getOrCreateRuntimeBinding`, `MAX_RUNTIME_BINDINGS`) plus background provisioning must be preserved. Model each workspace binding as a `createAgent()` instance (or a shared factory) keyed by `scope.key`. The two-handles rule (`00` invariant 12): the raw `x-boring-workspace-id` header (a surface/transport input) is resolved **adapter-side** to a `SessionCtx`. `SessionCtx { workspaceId?, userId? }` is boring's OWN runtime tenancy context (the `SessionStore` key) and is explicitly ALLOWED on the façade — it is **not** "platform addressing". Platform addressing = surface-native identifiers only (Slack team/channel/thread ts, workbook/sheet ids, workspace pane ids), which never reach the façade. So the façade legitimately sees `sessionId` and `SessionCtx`. **`workspaceId` is OPTIONAL**: a workspace HTTP adapter supplies it, but a pure/headless surface omits it entirely — the store then keys by `sessionStorageRoot`. **Invariant: a surface must never synthesize a fake `workspaceId` to satisfy the store** (a synthesized tenancy id is worse than none — it collides across surfaces and leaks a false tenant boundary).
  - Route wiring stays in the adapters. Verified current route imports/registers:
    - `createAgentApp.ts`: `healthRoutes` lines 233–236; file/fs-events/tree/search/git lines 238–252; `piChatRoutes` line 260; `systemPromptRoutes` line 261; `modelsRoutes` line 262; `skillsRoutes` lines 263–277; `sessionChangesRoutes` line 278; `catalogRoutes` line 279; `commandsRoutes` line 280; `reloadRoutes` lines 281–288; `readyStatusRoutes` line 289.
    - `registerAgentRoutes.ts`: optional `healthRoutes` lines 922–930; file/fs-events/tree/search/git lines 933–948; `piChatRoutes` lines 949–961; `systemPromptRoutes` lines 962–964; `modelsRoutes` line 965; `skillsRoutes` lines 966–996; `sessionChangesRoutes` line 997; inline reload route lines 998–1045; `catalogRoutes` lines 1046–1049; `commandsRoutes` lines 1050–1061; `readyStatusRoutes` lines 1062–1064.
  - `piChatRoutes` consumes the same `HarnessPiChatService` the façade builds — thread the service/agent instance through rather than constructing two services for one scope.
  - Preserve `registerCapabilitiesContributor('agent', …)` (`registerAgentRoutes.ts:866–881`) and the inline reload route (`registerAgentRoutes.ts:998–1045`) semantics exactly.
- **Tests to add:** none new here beyond keeping existing green — the whole point is behavior parity. Add a note in the PR that BBP1-006 + existing suites are the guardrail.
- **Acceptance:** `pnpm --filter @hachej/boring-agent run test` and `pnpm --filter @hachej/boring-agent run test:e2e` pass unchanged; a route-surface grep or route-list artifact in the PR description shows no route added/removed for existing `direct`/`local`/`vercel-sandbox` modes; CLI hub (`packages/cli/src/server/modeApps.ts`), workspace (`packages/workspace/src/app/server/createWorkspaceAgentServer.ts`), and core (`packages/core/src/app/server/createCoreWorkspaceAgentServer.ts`) still boot with their existing calls unchanged.

### BBP1-004 — `runtime: 'none'` pure path + `sessionStorageRoot` separation — M

- **Description:** Add the pure composition path (`runtime: 'none'`, no bash bundle spread into `tools`) and separate transcript/session storage (`sessionStorageRoot`) from workspace file roots.
- **PR-PLAN mapping:** `PR-PLAN.md` P1 row `pr4-pure-runtime-none` (`BBP1-004`, gates: pure route/tool exclusion, session round-trip under `sessionStorageRoot` with `workspaceId` undefined, no cwd leak; `lint:invariants`).
- **Files to touch:** `packages/agent/src/server/createAgent.ts` (pure branch); `packages/agent/src/server/harness/pi-coding-agent/createHarness.ts` / `sessions.ts` (accept an explicit session root distinct from `cwd`); adapters (`createAgentApp.ts`, `registerAgentRoutes.ts`) to expose a way to select pure mode.
- **Implementation notes:**
  - Adapter selector pin: add `runtime?: 'none'` to `CreateAgentAppOptions` and `RegisterAgentRoutesOptions` as the HTTP pure-mode selector. It is invalid to combine `runtime: 'none'` with `mode` or `runtimeModeAdapter`; throw a stable-code config error before building the app. Existing callers that omit `runtime` behave exactly as today.
  - Pure HTTP route set pin: register `healthRoutes`, `readyStatusRoutes`, `piChatRoutes`, `systemPromptRoutes`, `modelsRoutes`, `sessionChangesRoutes`, `catalogRoutes`, and `commandsRoutes` only. Do **not** register `fileRoutes`, `fsEventsRoutes`, `treeRoutes`, `searchRoutes`, `gitRoutes`, `skillsRoutes`, or `reloadRoutes` in pure mode. Rationale: `skillsRoutes` and reload/resource diagnostics can read cwd/Pi resources; P1 pure mode must prove no host file authority. A later bead may add an explicit non-file skills source, but P1 must not.
  - Pure tools pin: include only `config.tools ?? []` and host-configured non-bash operational tools. Do **not** call `buildHarnessAgentTools`, `buildFilesystemAgentTools`, `buildUploadAgentTools`, or `loadPlugins({ cwd })` in pure mode unless the exact tool is proven non-file/non-shell and app-owned. In particular, no `read/write/edit/find/grep/ls`, no `bash`, no `execute_isolated_code`, no upload/runtime artifact tools.
  - No `modeAdapter.create()` call, no `RuntimeBundle`, no `Workspace`, no `Sandbox`. `HarnessPiChatService` currently requires `workdir: string` and optional `workspace` (`harnessPiChatService.ts:45–59`). P1 choice: make `workdir` optional in `HarnessPiChatServiceOptions` / `RunContext` for pure mode and only pass it to harness adapters when present; do **not** pass `process.cwd()` or the repo root as a fake value. If Pi requires a cwd, pass a sealed temp/virtual root created under `sessionStorageRoot/.sealed-cwd` with no host files and document it in `_p1-pi-harness-audit.md`.
  - `sessionStorageRoot`: current `PiSessionStore` derives default session dirs through `sessionBaseDir(explicitRoot)` / `defaultSessionDir(cwd, explicitRoot)` at `sessions.ts:41–55` and already accepts `sessionRoot` / `sessionDir` at `sessions.ts:91–98`. Add `sessionStorageRoot` on `AgentConfig`; flow it to `AgentHarnessFactoryInput.sessionRoot` and `PiSessionStoreOptions.sessionRoot`, decoupled from `cwd`. Host reads `BORING_AGENT_SESSION_ROOT`; the façade takes the resolved path.
  - **Optional `workspaceId` (SessionCtx):** make `SessionCtx.workspaceId` optional in `packages/agent/src/shared/session.ts`, `PiSessionStore`, `HarnessPiChatService`'s `PiSessionStoreLike.loadEntries` type (`harnessPiChatService.ts:27–29`), `toSessionCtx(...)` (`harnessPiChatService.ts:681–682`), `PiSessionRequestContext.workspaceId` (`piSessionIdentity.ts:7–12`), and `PiSessionMetadata.workspaceId` (`piSessionIdentity.ts:28–37`). Pin the identity-service shape: keep `PiSessionMetadataIndex.listByWorkspace`, but change its signature to accept `workspaceId: string | undefined` and filter exact optional equality (`entry.workspaceId === workspaceId`), with `belongsToContext(...)` continuing to compare both optional `workspaceId` and `storageScope`. When `workspaceId` is present the store may namespace by it as today; when it is **absent** (pure/headless session) the store namespaces **solely by `sessionStorageRoot`** — no fabricated workspace id, no `process.cwd()` fallback, no synthetic tenant key. A pure session must `create → persist → list → load → delete` under `sessionStorageRoot` with `workspaceId` undefined throughout. Existing HTTP workspace routes may preserve `DEFAULT_WORKSPACE_ID` for compatibility, but `createAgent()` and pure-mode adapters must not synthesize it.
  - Skip boot-time plugin discovery (`loadPlugins`) in pure mode — it is already gated behind `workspaceFsCapability === 'strong'`, which `'none'` will not have; verify no other path calls it.
- **Tests to add:** pure-mode route-list test using Fastify's route list or injectable 404 assertions proves excluded routes (`/api/v1/files*`, fs-events, tree, search, git, skills, reload) are absent; tool catalog test proves fs/bash/upload tools are absent; session test creates/lists/loads/deletes under a temp `sessionStorageRoot` with no workspace root; no-cwd test asserts no `process.cwd()`/repo-root/`/workspace` path appears in pure config/prompt. **Optional-workspaceId test:** a pure session created with `SessionCtx.workspaceId` **undefined** persists under `sessionStorageRoot`, then reloads (list + load) in a fresh store instance pointed at the same `sessionStorageRoot` — round-trips with `workspaceId` still undefined and no synthesized id anywhere in the persisted record.
- **Acceptance:** `pnpm --filter @hachej/boring-agent run test -- createAgent.pure.test.ts` passes and its assertions prove `createAgent({ runtime: 'none' })` yields an agent with app-owned tools only, sessions under `sessionStorageRoot`, no synthesized `workspaceId`, and no file/bash routes or tools anywhere in pure mode.

### BBP1-005 — pi-coding-agent cwd/resource assumption audit → findings + seals — M

- **Description:** Audit `createPiCodingAgentHarness` and its resource/session/prompt paths for ambient host-file authority, since removing tools is insufficient if the harness still gets `process.cwd()` or reads `AGENTS.md`/global resources. Produce a findings doc and concrete follow-up "seals" (config flags / disable switches).
- **Files to touch/create:** create `docs/issues/391/runtime-refactor/work/P1-headless-core/_p1-pi-harness-audit.md` (findings); code seals land in `packages/agent/src/server/harness/pi-coding-agent/createHarness.ts` (+ `sessions.ts`, `pluginLoader.ts`) as the audit dictates.
- **PR-PLAN mapping:** `PR-PLAN.md` P1 row `pr5-pi-harness-audit` (`BBP1-005`, doc + seals, gates: `test`; harness-construction spy and prompt snapshot).
- **Implementation notes — known suspects to answer (from `01` "Pi harness audit"):**
  - **Session JSONL location:** `sessions.ts` `sessionBaseDir(explicitRoot)` defaults to `~/.pi/agent/sessions` unless `BORING_AGENT_SESSION_ROOT` (`sessions.ts:41–55`); `PiSessionStoreOptions` has `sessionRoot`/`sessionDir` (`:91–98`); `createPiCodingAgentHarness` builds `new PiSessionStore(opts.runtimeCwd ?? opts.cwd, { sessionRoot, sessionDir, storageCwd: opts.cwd })` (`createHarness.ts:365–370`). Native Pi sessions use `runtimeCwd = opts.runtimeCwd ?? ctx.workdir` (`:457`) and `SessionManager.open/create(..., runtimeCwd)` (`:461–467`). P1 seal: in pure mode, create/use exactly `join(sessionStorageRoot, ".sealed-cwd")` as the sealed empty root and pass it as both `cwd` and `runtimeCwd`; never pass `process.cwd()`, the repo root, or a workspace path. Also remove the hard-coded `const sessionCtx = { workspaceId: "default" }` at `createHarness.ts:453`; save Pi session files with the caller's optional `SessionCtx` instead.
  - **`.pi` extension loading:** `pluginLoader.ts` constants are `GLOBAL_DIR = ~/.pi/agent/extensions`, `LOCAL_DIR = .pi/extensions`, `EXTENSIONS_JSON = .pi/extensions.json` (`pluginLoader.ts:10–12`); npm discovery scans `node_modules/pi-plugin-*` at `:97–108`; `.pi/extensions.json` is read at `:142–153`, with npm/git entries handled at `:182–200`; `loadPlugins(...)` is at `:157–220`. P1 seal: pure mode does not call `loadPlugins` at all and does not pass `pi.extensionPaths`/`getHotReloadableResources` unless the host explicitly supplies sealed, app-owned resources.
  - **Skills dir + context files:** Boring already defaults `withPiHarnessDefaults(...)` to `noContextFiles: true` and `noSkills: true` (`createHarness.ts:119–133`). `DefaultResourceLoader({ cwd: opts.cwd, agentDir, ... })` is constructed at `:502–530`; the flags are applied at `:509–510`; if `noSkills` is true, `loadSkills({ cwd: opts.cwd, agentDir, skillPaths, includeDefaults: false })` runs at `:521–527`. P1 seal: pure mode keeps both defaults true and passes the sealed root as `opts.cwd`; do not re-enable ambient AGENTS.md/CLAUDE.md/user-global skill discovery in pure mode.
  - **Model registry / auth file:** `AuthStorage.create()` (`createHarness.ts:444`) and `getAgentDir()` (`:490`) read Pi-owned auth/settings roots; `createResourceSettingsManager(cwd, agentDir, piPackages)` calls `SettingsManager.create(cwd, agentDir)` when no package overlay exists (`:215–220`) and otherwise reads global/project settings through `agentDir/settings.json` and `cwd/.pi/settings.json` (`:222–247`); `models/modelConfig.ts:236` currently does `SettingsManager.create(process.cwd(), getAgentDir())`. P1 seal: Pi-owned auth/global settings reads are allowed, but every project-scoped settings read in pure mode must use the sealed root, never host cwd; move the `modelConfig.ts:236` `process.cwd()` dependency behind host-composed config before calling the façade.
  - **System prompt:** existing constants inject workspace/file/runtime guidance: `WORKSPACE_PATHS_GUIDELINE` (`createHarness.ts:44–62`) and `PYTHON_RUNTIME_GUIDELINE` (`:64–77`) are always appended by `composeSystemPromptAppend(...)` (`:79–82`), then used at `:486`. P1 seal: add `PiHarnessOptions.includeDefaultRuntimeGuidelines?: boolean` (default `true` for existing HTTP modes) and set it to `false` in `runtime: 'none'`; pure prompts must omit workspace-path, file-tool, cwd, `.boring-agent/venv`, and Python runtime guidance unless the host explicitly supplies equivalent app-owned tools.
  - **Compaction / session identity:** audit whether compaction assumes file tools exist, and update it to tolerate pure sessions with no file/bash tools. Session identity must accept optional `workspaceId` (BBP1-004) and use `sessionStorageRoot`/`storageScope` for workspace-less sessions; no identity path may synthesize `"default"`, `"workspace"`, or a cwd-derived tenant id.
- **Decision to record:** per `08` decision 2 + `00` open-decision 1, the outcome is **sealed pi harness, not a second harness**. The audit must show pi can run with cwd disabled/sealed, or escalate.
- **Tests to add:** harness-construction spy asserting no host cwd/path in pure mode; system-prompt snapshot with no cwd/workspace/AGENTS.md/file-tool text (extend the existing `packages/agent/src/server/__tests__/system-prompt-size.regression.test.ts` neighborhood or add a new snapshot test).
- **Acceptance:** `_p1-pi-harness-audit.md` exists and contains a table with rows for session JSONL, native Pi session cwd, plugin loading, context files, skills, auth/model settings, system prompt, compaction, and session identity; each row has `current source`, `verdict`, `P1 seal`, and `test/proof`, with zero `UNKNOWN` / `TBD` rows. `pnpm --filter @hachej/boring-agent run test -- createAgent.pure.test.ts runtimeCwd.test.ts` passes and proves sealed cwd + prompt omissions.

### BBP1-006 — Invariant + smoke tests — M

- **Description:** Lock the architecture with executable checks: (a) no agent value import from boring-bash; (b) no Fastify in the `createAgent()` module graph; (c) a plain-Node smoke test that `createAgent({ runtime: 'none' })` runs a turn with a fake harness.
- **PR-PLAN mapping:** `PR-PLAN.md` P1 row `pr6-invariants-smoke` (`BBP1-006`, test/script, gates: `lint:invariants`; `check:isolation`; `boring-bash check:invariants`).
- **Files to touch/create:**
  - extend root `scripts/check-invariants.sh` — add a source-level guard that `packages/agent/src/core/**` and `packages/agent/src/shared/events.ts` contain no value import/require of `fastify`.
  - extend `packages/agent/scripts/check-agent-isolation.ts` — keep the existing `@hachej/boring-core` dist scan and add a built-artifact graph walk from `dist/core/index.js` / `dist/core/index.mjs` that follows relative ESM imports through split chunks and fails on any reachable `fastify` specifier.
  - keep `packages/boring-bash/scripts/check-invariants.mjs`'s existing agent→boring-bash value-import scan (`packages/boring-bash/scripts/check-invariants.mjs:59–70`) as-is unless the checker needs a small helper for negative proof; do not move the Fastify graph proof into this script.
  - add `packages/agent/src/server/__tests__/createAgent.pure.test.ts` (vitest) — the smoke test.
- **Implementation notes:**
  - **Fastify-graph check:** `createAgent` ships behind its own **`@hachej/boring-agent/core` published subpath** (BBP1-002) precisely so its closure is separable from the adapters (which legitimately import Fastify in `dist/server/index.js`). Enforce the guarantee in two layers: direct source guard in `scripts/check-invariants.sh`, then built transitive closure in `check-agent-isolation.ts` after `pnpm --filter @hachej/boring-agent run build`. The dedicated subpath is required (do not try to keep `createAgent` inside the adapter barrel and graph-walk around it).
  - **Smoke test:** in plain Node/vitest (no Fastify), build a fake `AgentHarnessFactory` returning a minimal `AgentHarness` (in-memory `SessionStore`, a `getPiSessionAdapter` stub emitting one text event), call `createAgent({ runtime: 'none', harnessFactory: fake, tools: [echoTool] })`, drive `send()`, and assert: one turn completes, no file/bash tool in catalog, no `process.cwd()`/repo path in any config/prompt, session persisted under a temp `sessionStorageRoot`.
- **Tests to add:** the three checks above.
- **Acceptance:** `pnpm --filter @hachej/boring-agent run build`, `pnpm --filter @hachej/boring-agent run lint:invariants`, `pnpm --filter @hachej/boring-agent run check:isolation`, `pnpm --filter @hachej/boring-bash run check:invariants`, and `pnpm --filter @hachej/boring-agent run test -- createAgent.pure.test.ts` pass. The PR description includes a negative-proof artifact: a temporary value import from `@hachej/boring-bash` under `packages/agent/src/**` makes `pnpm --filter @hachej/boring-bash run check:invariants` fail, and a temporary `fastify` import reachable from `packages/agent/src/core/index.ts` makes both `lint:invariants` (source) and `check:isolation` (built graph, after build) fail. Do not commit the temporary mutations. This corrects the false older premise that the agent→boring-bash check would fail on the pre-refactor tree; it already exists and should pass today.

## Verification

Commands (verified to exist in the current package manifests):

- `pnpm --filter @hachej/boring-agent run build` — tsup build (must stay green; `build:assert` runs artifact assertions).
- `pnpm --filter @hachej/boring-agent run typecheck` — `tsc --noEmit`.
- `pnpm --filter @hachej/boring-agent run test` — vitest (all existing agent tests + new BBP1 tests).
- `pnpm --filter @hachej/boring-agent run lint:invariants` — `bash ../../scripts/check-invariants.sh .` (ripgrep invariant scan).
- `pnpm --filter @hachej/boring-bash run check:invariants` — runs `packages/boring-bash/scripts/check-invariants.mjs` (agent→boring-bash value-import check; negative-proof artifact required in BBP1-006).
- `pnpm --filter @hachej/boring-agent run check:isolation` — `check-agent-isolation.ts` dist scan (requires a build first; BBP1-006 extends it with the `/core` Fastify graph walk).
- `pnpm --filter @hachej/boring-agent run test:e2e` — Playwright e2e (`m2-modeflip`, `m3a-sessions`, `bridge-protocol`, `bombadil`); the parity guard for the adapter refactor.
- Root aggregate (optional, heavier): `pnpm lint:invariants` (runs agent + boring-bash + workspace-plugin invariants).
- Manual: start the cli hub and the workspace playground against the refactored build and confirm chat + file tree + sessions behave identically (see the repo's playground run recipe).

## Review gates

- Thermo architecture review must be clean (per `README.md` "Review rule"): no `boring-agent → boring-bash` cycle, no duplicated provisioning/readiness system, no fs/bash split brain, no hidden cwd/fs leak in pure mode.
- Precondition gate: **P0 must be merged**, and the P1 package must have thermo double-GREEN recorded before executor dispatch.
- BBP1-005's pi-harness audit findings must be reviewed and the "sealed pi, not second harness" decision confirmed **before** the pure-mode exit criteria are claimed (BBP1-004 depends on BBP1-005's seals).
- Behavior-parity gate: the reviewer must confirm the existing agent unit + e2e suites pass unchanged, and that no existing route was added/removed for `direct`/`local`/`vercel-sandbox` modes.
- The Fastify-graph invariant (BBP1-006) must actually fail when a `fastify` import is introduced into the façade closure — reviewer verifies the negative case.
- Do not start Track T1 (durable event-store append/replay, historical `stream`, approvals/`resolveInput`) until BBP1-002..006 are merged; T1 depends on the P1 `AgentEvent` envelope and stub seams landing here.
