# VISION-MAP — from today's codebase to the north star

One page for the dispatching orchestrator: each vision component (00 "North star": eve-class UX, workspace as control plane, Flue internals, EU-sovereign, multi-fs + flexible sandbox + headless agent) mapped to **what exists today → the delta work orders → a checkable end-state**. If every end-state assertion below passes, the vision is delivered; anything not listed here is out of scope for this epic.

| # | Vision component | Exists today | Delta (work orders) | Checkable end-state |
|---|---|---|---|---|
| 1 | **Headless agent** — pure config object, no fs/bash/HTTP/React | Transport-agnostic harness seam (`HarnessPiChatService` via `getPiSessionAdapter`) exists but only delivered through Fastify (`createAgentApp`/`registerAgentRoutes`); config sprawls across env vars + file discovery | `TODO-P0` → `TODO-P1` | `createAgent({ runtime: 'none' })` runs a full turn in a plain Node script with zero Fastify/boring-bash imports; all current HTTP consumers unchanged |
| 2 | **Multi-fs** — governed named filesystems attached per agent/session | **Landed (#416)**: `FilesystemBinding` contracts, `user` + readonly `company_context`, projection ops, no-leak conformance in `packages/boring-bash` | `TODO-E1` (generalized `Environment`/attachments, scoped views, realpath+symlink hardening) → `TODO-P4` (file UI move, Company tree preserved) | An agent holds ≥2 filesystems with distinct identities; a scoped-view attachment passes the symlink-escape test; company_context no-leak conformance green throughout |
| 3 | **Flexible sandbox** — swappable exec providers, honest capabilities | direct/bwrap/vercel-sandbox behind `Sandbox`/`RuntimeBundle` — but inside `packages/agent`, with static capability claims | `TODO-P2` (providers → dedicated **`@hachej/boring-sandbox`** package `packages/boring-sandbox/src/providers`; `resolveMode` → boring-bash; capabilities as reported\|unknown facts, fail closed) → `TODO-P5` (provisioning, secret brokering: host-side broker handles only; no brokered secret ever enters a sandboxed environment — the direct provider is a host process, not a sandbox) | Provider swap requires no agent-package change; boring-sandbox imports agent types only and boring-bash imports boring-sandbox values (acyclic); a test proves no brokered secret is readable inside a sandbox; remote-worker capabilities come only from handshake |
| 4 | **External agent access** — any MCP client mounts an environment | None (MCP used client-side only, `plugins/boring-mcp`) | `TODO-E2` (MCP projection over existing projection ops, token→`BoundFilesystemContext` identity) | An external MCP client (e.g. Claude Code) mounts a boring environment; denied files absent over MCP; no-leak suite green on the MCP mount |
| 5 | **Flue building blocks** — durable replayable streams + channels-for-free | Bespoke replay (`PiChatReplayBuffer` + `?cursor=` NDJSON); no channel adapters | `TODO-T1` (DS protocol: SQLite `EventStreamStore` + DS routes + approvals-on-stream) → `TODO-T2` (transport contract, front refit) → `TODO-S1` (Slack via `@flue/slack` ingress) | SSE drop reconnects losslessly by `offset`; an approval raised in one client is answered from another; a Slack thread and the workspace UI are served by the same agent + session store |
| 6 | **eve UX — workspace as control plane** | Partial: `SessionList`/`SessionBrowser` + search, `DebugDrawer` (event inspector), ask-user plugin UI, model pickers/readiness badges. No agent registry, no cross-surface view, no unified approvals | `TODO-P6` (`AgentRegistry`, plugin/child-app scoping) → `TODO-P7` (agentId routing + `GET /api/v1/agents/:agentId/info`) → `TODO-S3` (delta panels: agent inspect [new], cross-surface sessions [extend SessionList], approval inbox [generalize ask-user]) | Workspace lists/inspects agents from the info endpoint; a Slack-born session is observable in the workspace by `sessionId`; a pending approval from any surface is answerable from the workspace inbox |
| 7 | **Embeds** — agent inside another product (pi-excel) | None | `TODO-S2` (embed contract + reference embed, host-supplied domain tools, `runtime: 'none'`) | The reference embed runs with zero boring-bash dependency; host tools render; approvals work via the host dialog |
| 8 | **EU-sovereign hosting** | Implicitly true (SQLite, bwrap, self-hostable) but unstated | Invariant 15 (00) enforced across every work order (todos-v2/README non-negotiable) | Default stack (event store, sessions, sandboxes, workers, channel egress) deploys on EU infra with no US-hosted hard dependency; `vercel-sandbox` is optional |
| 9 | **The farm (next epic — deferred; does NOT gate this epic's exit)** | boring-tasks kanban board (PR #486: presentation + source adapters, GitHub Issues as durable source; no agent/session linkage yet) + this epic's substrate (runtime-owned `sessionId`, `agentId` scoping [P7], replayable streams [T1], `sessions.pendingInputs`, S3/FUSE mounts [X1], reserved `data-artifact` part [08]) | **#397 durable task service exposed as a boring-tasks source** (assignment/claim, wake/run lifecycle, supervision) + the **farm epic** (fleet view, artifact shelf folding `data-artifact` parts, Farm MCP control-plane for foreign agents) | A foreign agent creates a task, works it in a mounted env, publishes an artifact, requests approval — all visible in the workspace (this epic guarantees only the substrate; the assertion itself lands with the farm epic) |

> **Business line (farm row context):** the farm (row 9) is **Horizon-1 INTERNAL leverage** — the factory that delivers vertical-agent client work (Engagement Analyst, MacroAnalyst on sovereign tenants), not a product sold yet. It becomes client-facing only at Horizon 2 (white-label workroom, post 3+ repeats), and the hub-and-spoke CLI⇄hosted-agents shape is Horizon 3 (2027+). One deployable artifact; topology is the product line. See `00-global-isa.md` "Business horizons."

## Explicitly deferred (do not build in this epic)

- **Agent-as-directory authoring** (eve `defineAgent` file conventions) — north-star DX; unblocked after P7's `AgentRegistry`; file a dedicated issue at P8.
- `FileTreeDataProvider` boundary — until #295 is scheduled.
- Subagent environment grants — first consumer lands in P7 (kept minimal there).
- Durable turn continuation (WaitingTurn machine) — restart-resume is new-turn-seeded by design (T1); revisit only if a real workflow need appears.

## Dispatch order (same graph as todos-v2/README)

P0 → P1 → { T1 → T2 → S1 → S2 } ∥ { P2 → P3 → [P4, E1 → E2, P5 → P6 → P7 → [P8, S3]] }

(P5 dispatches off P3 in parallel with P4 and E1→E2 — it does not wait on P4 or E1. P8 gates on **all** lanes.)

Rule of thumb for the orchestrator: rows 1→5 are infrastructure and can run as three parallel lanes after P1; rows 6–7 are product payoff and gate on the lanes; row 8 is a standing constraint, not a phase.
