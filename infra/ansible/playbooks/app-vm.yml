---
- name: Configure boring-ui App VM
  hosts: app
  become: true
  roles:
    - role: common
    - role: docker
      when: docker_enabled | default(true)
    - role: cloudflared
      when: cloudflared_enabled | default(true)
