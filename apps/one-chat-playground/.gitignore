# Playwright screenshots and other local proof artifacts.
.artifacts/
# Pi session transcripts are mutable host data, not source.
.boring-agent/
test-results/
playwright-report/
# Scratch copy of sample-app used to drive the app without touching the template.
.test-workspace/
